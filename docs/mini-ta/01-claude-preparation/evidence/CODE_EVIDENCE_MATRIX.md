# Code Evidence Matrix

Sumber: raw JSON terverifikasi di `docs/mini-ta/01-claude-preparation/evidence/_raw-audit-json/`, ditambah evidence targeted dari `README.md` dan `Cargo.toml` untuk tujuan/platform/dependency. Klaim dengan verdict `REFUTED` tidak dimasukkan.

| ID | Klaim | Evidence | Status | Confidence | Catatan |
|----|-------|----------|--------|------------|---------|
| CB-001 | AKSARA dideskripsikan sebagai chat P2P terminal terenkripsi dan serverless untuk dua orang, tanpa server perantara. | README.md:23-23; module=project_docs; symbol=README purpose statement; evidence=README baris 23 menyatakan chat P2P terminal terenkripsi/serverless dan dua orang chat end-to-end tanpa server perantara. | DOCUMENTED_ONLY | HIGH |  |
| CB-002 | README menyediakan artefak rilis untuk Windows, Linux, dan macOS Apple Silicon. | README.md:52-54; module=project_docs; symbol=release platform table; evidence=Tabel instalasi README baris 52-54 mencantumkan binary Windows, Linux, dan macOS. | DOCUMENTED_ONLY | HIGH |  |
| CB-003 | Paket Rust bernama aksara memakai edition 2021 dan minimum rust-version 1.89. | Cargo.toml:2-7; module=manifest; symbol=package metadata; evidence=Cargo.toml mendefinisikan package name, edition 2021, dan rust-version 1.89. | IMPLEMENTED | HIGH |  |
| CB-004 | Runtime dan UI utama memakai tokio multi-thread async runtime, ratatui, dan crossterm; transport memakai mdns-sd untuk LAN dan arti-client/tor stack untuk Tor. | Cargo.toml:42-70; module=manifest; symbol=dependency set; evidence=Cargo.toml mencantumkan tokio, ratatui, crossterm, mdns-sd, arti-client, tor-rtcompat. | IMPLEMENTED | HIGH |  |
| CB-005 | Mode default pemakaian adalah ONLINE dengan LAN + Tor, sedangkan --offline menjalankan LAN murni tanpa Tor. | README.md:92-96; module=project_docs; symbol=usage examples; evidence=README baris 92-96 menunjukkan aksara default ONLINE LAN+Tor dan --offline untuk LAN murni. | DOCUMENTED_ONLY | HIGH |  |
| CB-006 | Ketiga fungsi itu memang ada di file ini (fingerprint baris 48-54, invite encode/decode baris 59-92, penyimpanan terenkripsi baris 170-198), tapi baris 1-14 yang dikutip sebagai evidence adalah komentar modul yang untuk bagian penyimpanan justru menyatakan SEBALIKNYA: 'contact store hidup di RAM saja, tidak dipersist ke disk' (baris 11-14, catatan M1 yang sudah usang). Baris 1-14 tidak mendukung klaim 'penyimpanan kontak terenkripsi' — evidence yang tepat untuk itu ada di baris 170-198, bukan 1-14. | src/contacts/mod.rs:1-14; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CORRECTED. | IMPLEMENTED | HIGH | Klaim benar tentang implementasi aktual, tapi rentang evidence yang diberikan justru berisi teks yang bertentangan dengan salah satu dari tiga fungsi yang diklaim. |
| CB-007 | Struct Contact menyimpan nickname (String), ed25519_pub ([u8;32]), noise_pub ([u8;32]), dan onion (Option<String>, None = LAN-only) — evidence baris 30-37. | src/contacts/mod.rs:30-37; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-008 | Komentar modul (baris 11-14) menyatakan contact store hidup di RAM saja / tidak dipersist di M1, padahal file yang sama sudah mengimplementasikan persistensi terenkripsi penuh (save_contacts/load_contacts) di baris 94-198 — dokumentasi tidak sinkron dengan implementasi. | src/contacts/mod.rs:11-198; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | INCONSISTENT | HIGH | Kemungkinan komentar ditulis sebelum fitur persistensi ditambahkan dan belum diperbarui. Baris 11-14 persis berisi teks 'Catatan M1: contact store hidup di RAM saja, tidak dipersist ke disk... Persistensi terenkripsi (memperluas vault) adalah follow-up M1.x/M2.' Bertentangan langsung dengan save_contacts/load_contacts di baris 170-198. |
| CB-009 | Komentar modul (baris 9) menyatakan 'Onion address akan ditambahkan di M2' (future tense), tetapi kode sudah mendukung onion penuh (field Contact.onion, parameter onion di encode/decode_invite, test terkait) — cross-check baris 36, 59-68, 215-225. | src/contacts/mod.rs:8-9; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | INCONSISTENT | MEDIUM | Cross-check dengan baris 36, 59-68, 215-225 yang menunjukkan onion sudah terimplementasi. Baris 8-9 persis: '...out-of-band (bukan lewat aplikasi). Onion address akan ditambahkan di M2.' Field onion (baris 36), encode_invite/decode_invite dengan parameter onion (baris 59-68), dan test invite_roundtrip_with_onion (baris 216-225) semuanya terverifikasi ada. |
| CB-010 | Format invite code = base64(ed25519_pub[32] \|\| noise_pub[32]) URL-safe tanpa padding, onion opsional di-append sebagai suffix '@xxxx.onion', tanpa magic prefix disengaja — evidence baris 56-68. | src/contacts/mod.rs:56-68; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-011 | decode_invite: split di karakter '@' pertama, decode base64, wajib hasil persis 64 byte — gagal salah satunya menghasilkan Error::InvalidInvite — evidence baris 74-92. | src/contacts/mod.rs:74-92; module=contacts; symbol=Error::InvalidInvite; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-012 | Format file kontak terenkripsi di disk = [12 byte nonce][ciphertext+tag], tanpa magic bytes header, sesuai komentar section 'Persistensi terenkripsi' — evidence baris 94-100. | src/contacts/mod.rs:94-100; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-013 | Format flat-text-nya akurat dan sesuai serialize_contacts di baris 114-128 (fungsi ini murni menulis, tanpa splitn). Detail 'splitn(4, ' ')' bukan bagian dari baris 114-128 — itu ada di deserialize_contacts, tepatnya baris 146 (dalam rentang 140-167), sisi parsing/baca, bukan sisi serialisasi/tulis. | src/contacts/mod.rs:114-128; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CORRECTED. | IMPLEMENTED | HIGH | Isi klaim benar secara keseluruhan tapi mencampur dua fungsi berbeda (serialize vs deserialize) di bawah satu evidence range yang hanya mencakup salah satunya. |
| CB-014 | Nickname kontak di-strip dari '\n'/'\r' sebelum diserialisasi untuk mencegah record/line injection — evidence baris 118. | src/contacts/mod.rs:118-118; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-015 | fingerprint() = hex(BLAKE2s256(ed25519_pub \|\| noise_pub \|\| "aksara-fingerprint-v1")), 64 karakter hex; kedua public key wajib diserap bersama mencegah serangan invite susun-ulang — evidence baris 39-54. | src/contacts/mod.rs:39-54; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-016 | Frasa 'Invite code tidak ditandatangani' ada di baris 42, bukan baris 43 seperti disebut klaim (baris 42: 'Kedua key WAJIB ikut diserap. Invite code tidak ditandatangani, jadi kalau...'). Rentang evidence keseluruhan 42-47 sudah tepat dan mencakup baris 42; hanya sitasi baris spesifik di dalam teks klaim yang keliru. | src/contacts/mod.rs:42-47; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CORRECTED. | IMPLEMENTED | HIGH | Ini bukan klaim bahwa sistem aman/tidak aman secara keseluruhan, hanya deskripsi mekanisme yang ada di file ini. Substansi klaim (tidak ditandatangani, verifikasi out-of-band) sepenuhnya akurat — hanya off-by-one pada nomor baris kutipan. |
| CB-017 | derive_contacts_key() menurunkan kunci dari bundle.identity.secret_bytes() (bukan passphrase) via BLAKE2s256; komentar eksplisit menyatakan ini sengaja bukan Argon2id — evidence baris 102-112. | src/contacts/mod.rs:102-112; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-018 | save_contacts memetakan kegagalan enkripsi AEAD ke Error::Encryption, kegagalan std::fs::write ke Error::Io — evidence baris 174-180. | src/contacts/mod.rs:174-180; module=contacts; symbol=Error::Encryption; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-019 | load_contacts memperlakukan SETIAP error dari std::fs::read sebagai 'file tidak ada' -> Ok(Vec::new()), termasuk kemungkinan permission denied — evidence baris 185-188. | src/contacts/mod.rs:185-188; module=contacts; symbol=std::fs; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-020 | load_contacts memperlakukan file lebih pendek dari C_NONCE_LEN+16 byte sebagai daftar kontak kosong, bukan error korupsi — evidence baris 189-191. | src/contacts/mod.rs:189-191; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-021 | Kegagalan dekripsi/verifikasi tag AEAD (kunci salah maupun data corrupt) dipetakan ke Error::Decryption yang sama, tidak dibedakan pada level pesan error — evidence baris 194-196. | src/contacts/mod.rs:194-196; module=contacts; symbol=Error::Decryption; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-022 | deserialize_contacts diam-diam skip baris yang tidak punya 4 field lengkap atau field hex key invalid/salah panjang, tanpa melaporkan ke caller — evidence baris 140-167. | src/contacts/mod.rs:140-167; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-023 | Alamat onion diperlakukan sebagai string opaque tanpa validasi format (suffix '.onion', panjang, karakter valid), baik saat encode/decode invite maupun serialize/deserialize contacts — evidence baris 59-163. | src/contacts/mod.rs:59-163; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | MEDIUM |  |
| CB-024 | Tidak ada penggunaan Zeroize/ZeroizeOnDrop di file ini: Contact tidak men-derive Zeroize (baris 30-37), derive_contacts_key mengembalikan [u8;32] polos (baris 104-112), buffer plaintext String hasil dekripsi tidak dibersihkan (baris 197) — evidence baris 104-197. | src/contacts/mod.rs:104-197; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | NOT_FOUND | MEDIUM | Zeroize 1.9.0 ada di dependency graph proyek, tapi tidak dipakai untuk tipe-tipe di file khusus ini; kemungkinan diterapkan di modul identity yang tidak dibaca di sini. Tidak ada import zeroize sama sekali di file ini (baris 16-27 hanya path/base64/blake2/chacha20poly1305/rand/crate-internal). Semua sitasi baris inline (30-37, 104-112, 197) akurat, meski ... |
| CB-025 | Komentar (baris 40) menyatakan fingerprint 'dipakai untuk matching saat discovery dan untuk menentukan role handshake' — hanya deskripsi komentar, tidak dieksekusi di file ini sendiri — evidence baris 40-41. | src/contacts/mod.rs:40-41; module=contacts; symbol=mod module; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | DOCUMENTED_ONLY | LOW | Butuh cross-check ke modul transport/discovery untuk konfirmasi pemakaian aktual. |
| CB-026 | derive_contacts_key memanggil bundle.identity.secret_bytes() dari KeyBundle (didefinisikan di crate::identity::keypair, tidak dibaca di audit ini) — bentuk/panjang persis secret_bytes() tidak bisa dikonfirmasi hanya dari file ini — evidence baris 27-106. | src/contacts/mod.rs:27-106; module=contacts; symbol=crate::identity; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | NEEDS_CONFIRMATION | LOW |  |
| CB-027 | Test invite_roundtrip_lan_only memverifikasi encode/decode_invite round-trip ed25519_pub & noise_pub identik, onion None saat tidak diberikan — evidence baris 204-213. | src/contacts/mod.rs:204-213; module=contacts; symbol=invite_roundtrip_lan_only; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-028 | Test invite_roundtrip_with_onion memverifikasi round-trip encode/decode invite dengan onion disertakan, onion ikut terbawa balik dengan benar — evidence baris 215-225. | src/contacts/mod.rs:215-225; module=contacts; symbol=invite_roundtrip_with_onion; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-029 | Test invite_rejects_garbage memverifikasi decode_invite Err untuk string bukan base64/invite valid — evidence baris 227-230. | src/contacts/mod.rs:227-230; module=contacts; symbol=invite_rejects_garbage; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-030 | Test invite_rejects_wrong_length memverifikasi decode_invite Err ketika base64 valid tapi hasil decode bukan 64 byte (encode dari array 10 byte) — evidence baris 232-237. | src/contacts/mod.rs:232-237; module=contacts; symbol=invite_rejects_wrong_length; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-031 | Test contacts_save_load_roundtrip memverifikasi save_contacts+load_contacts pada 2 kontak (satu nickname mengandung spasi & onion Some, lainnya onion None) identik setelah round-trip via file sementara — evidence baris 239-269. | src/contacts/mod.rs:239-269; module=contacts; symbol=contacts_save_load_roundtrip; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-032 | Test contacts_load_missing_file_is_empty memverifikasi load_contacts pada path tidak ada mengembalikan Ok dengan Vec kosong — evidence baris 271-276. | src/contacts/mod.rs:271-276; module=contacts; symbol=contacts_load_missing_file_is_empty; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-033 | Test contacts_wrong_key_fails memverifikasi load_contacts dengan kunci berbeda dari saat save menghasilkan Err — evidence baris 278-292. | src/contacts/mod.rs:278-292; module=contacts; symbol=contacts_wrong_key_fails; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-034 | Test fingerprint_is_64_hex_chars memverifikasi output fingerprint() selalu 64 karakter hex ASCII valid — evidence baris 294-299. | src/contacts/mod.rs:294-299; module=contacts; symbol=fingerprint_is_64_hex_chars; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-035 | Test fingerprint_binds_both_keys memverifikasi mengubah HANYA noise_pub menghasilkan fingerprint berbeda, dan sebaliknya mengubah ed dengan noise tetap juga berbeda — evidence baris 303-314. | src/contacts/mod.rs:303-314; module=contacts; symbol=fingerprint_binds_both_keys; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-036 | Test invite_has_no_obvious_prefix memverifikasi dua invite dari identitas berbeda punya 4 karakter awal berbeda, tidak ada prefix/magic byte konstan — evidence baris 316-323. | src/contacts/mod.rs:316-323; module=contacts; symbol=invite_has_no_obvious_prefix; evidence=Verified JSON module=contacts; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-037 | architecture: Noise_IK pattern 'Noise_IK_25519_ChaChaPoly_BLAKE2s' via snow, dipakai membangun HandshakeState (initiator/responder). | src/crypto/handshake.rs:3-21; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Baris 3 komentar pattern name persis cocok, baris 18 import Builder/HandshakeState/TransportState dari snow, baris 21 const NOISE_PATTERN berisi string yang sama persis. |
| CB-038 | Header lines 9-12 dan isi do_handshake() (baris 109-129) akurat. Namun catatan 'dipakai oleh keempat test' SALAH: do_handshake() hanya dipanggil di 3 dari 4 test — handshake_ik_roundtrip (baris 144), transport_encrypt_decrypt_roundtrip (baris 159), responder_verifies_initiator_identity (baris 186). Test keempat, wrong_peer_key_fails_handshake, TIDAK memanggil do_handshake() sama sekali — ia menjalankan write_message/read_message secara manual hanya untuk message 1 (baris 209-216), karena tujuannya memang menguji kegagalan sebelum message 2 terkirim. | src/crypto/handshake.rs:9-12; module=crypto_handshake; symbol=keempat; evidence=Verified JSON module=crypto_handshake; verdict=CORRECTED. | IMPLEMENTED | HIGH | Verifikasi empiris ada di do_handshake() baris 109-129, dipakai oleh keempat test. Diverifikasi via grep 'do_handshake' — hanya muncul di baris 109 (definisi) dan 144, 159, 186 (pemanggilan). Baris 193-217 (wrong_peer_key_fails_handshake) tidak menyebut do_handshake. |
| CB-039 | identity: new_initiator menerima local_noise_privkey & peer_noise_pubkey sebagai &[u8;32], dibangun via snow::Builder dengan local_private_key()/remote_public_key(). | src/crypto/handshake.rs:32-38; module=crypto_handshake; symbol=snow::Builder; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-040 | identity: new_responder hanya menerima local_noise_privkey (&[u8;32]); peer pubkey belum diketahui di awal, khas pola IK. | src/crypto/handshake.rs:40-48; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Sesuai komentar baris 42: 'Responder tidak perlu tahu peer's pubkey di awal — akan diverifikasi dari message 1.' |
| CB-041 | data_flow: write_message()/read_message() adalah wrapper tipis ke snow::HandshakeState, error dipetakan via Error::Noise. | src/crypto/handshake.rs:52-60; module=crypto_handshake; symbol=snow::HandshakeState; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-042 | session_mgmt: into_transport() mengonsumsi HandshakeSession by value, memanggil into_transport_mode(), menghasilkan EncryptedSession; komentar menyatakan memanggil sebelum selesai = error. | src/crypto/handshake.rs:68-73; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Komentar baris 69 persis: 'Memanggil ini sebelum handshake selesai akan menghasilkan error.' |
| CB-043 | auth: remote_static() mengembalikan Option<&[u8]> (slice, bukan array tetap), diambil setelah handshake selesai. | src/crypto/handshake.rs:75-79; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-044 | Fakta baris & perilaku (bob.read_message() mengembalikan Err ketika alice pakai eve's pubkey) akurat. Tapi framing 'autentikasi static-key INITIATOR' kurang presisi: token DH 'es' pada Noise_IK adalah DH(initiator ephemeral, RESPONDER static) — ini mengautentikasi secara implisit apakah initiator benar-benar menghubungi RESPONDER yang benar (identitas bob), bukan memverifikasi static key milik initiator sendiri (alice). Kegagalan terjadi karena 'es' yang dihitung alice (pakai eve_pub) tidak cocok dengan 'es' yang dihitung bob (pakai private key bob sendiri), sehingga dekripsi field 's'/payload message 1 gagal di sisi bob. | src/crypto/handshake.rs:193-217; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CORRECTED. | IMPLEMENTED | HIGH | Komentar kode sendiri (baris 199-214) menggunakan framing serupa ('Bob tidak bisa dekripsi'), jadi ini bukan fabrikasi — hanya istilah 'autentikasi static-key initiator' yang menyesatkan soal properti keamanan mana yang sebenarnya diuji. |
| CB-045 | auth: hanya responder (bob) yang diuji memanggil remote_static() (test responder_verifies_initiator_identity); tidak ada test yang memanggil remote_static() di sisi initiator. | src/crypto/handshake.rs:174-191; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | NEEDS_CONFIRMATION | HIGH | Fakta yang diverifikasi: tidak ada pemanggilan alice.remote_static() di seluruh file test. API remote_static() sendiri generik dan bisa dipanggil kedua sisi (baris 77-79). Diverifikasi via grep 'remote_static' pada seluruh file — hanya muncul di definisi (baris 77-78) dan satu pemanggilan bob.remote_static() (baris 189). Tidak ada alice.remote_static() di... |
| CB-046 | limitation: Forward secrecy & Identity hiding hanya disebut di komentar header, tidak ada test eksplisit yang memverifikasinya. | src/crypto/handshake.rs:14-17; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | DOCUMENTED_ONLY | MEDIUM | Baris 14-17 persis berisi ketiga properti keamanan tsb. Ditinjau keempat test (131-217): tidak ada yang membandingkan ephemeral key antar sesi atau memeriksa ciphertext static-key. |
| CB-047 | testing: is_finished() hanya dikompilasi di bawah #[cfg(test)], tidak tersedia di runtime produksi. | src/crypto/handshake.rs:62-66; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-048 | data_flow: EncryptedSession::encrypt()/decrypt() wrapper tipis di atas snow::TransportState; komentar menyatakan buffer output butuh plaintext.len()+16 untuk overhead AEAD. | src/crypto/handshake.rs:88-98; module=crypto_handshake; symbol=EncryptedSession::encrypt; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Komentar baris 90 persis: "`out` harus punya kapasitas setidaknya `plaintext.len() + 16` (overhead AEAD)." |
| CB-049 | error_handling: semua error dari write_message/read_message/into_transport_mode dipetakan seragam ke Error::Noise via .map_err(Error::Noise); tidak ada diferensiasi jenis kegagalan. | src/crypto/handshake.rs:53-97; module=crypto_handshake; symbol=Error::Noise; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Definisi persis varian Error::Noise ada di src/error.rs yang tidak dibaca dalam audit ini — evidence_symbol Error::Noise merujuk situs pemakaian, bukan definisi. Diverifikasi via grep: map_err(Error::Noise) muncul persis 5 kali (baris 53, 59, 71, 92, 97), semua identik tanpa diferensiasi. |
| CB-050 | error_handling: Builder::new(NOISE_PATTERN.parse()?) dan pemanggilan Builder lain pakai operator '?'; tipe error asli snow tidak terlihat di file ini. | src/crypto/handshake.rs:33-36; module=crypto_handshake; symbol=Builder::new; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | NEEDS_CONFIRMATION | MEDIUM | 4 pemakaian '?' pada baris 33-36 (parse, local_private_key, remote_public_key, build_initiator). Tidak ada impl From<snow::Error> for Error terlihat di file ini. |
| CB-051 | logging: tidak ditemukan pemanggilan log::/tracing::/println! di seluruh handshake.rs. | src/crypto/handshake.rs:1-219; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | NOT_FOUND | HIGH |  |
| CB-052 | limitation: tidak ditemukan penggunaan zeroize/Zeroize/ZeroizeOnDrop pada HandshakeSession/EncryptedSession maupun field state-nya di handshake.rs. | src/crypto/handshake.rs:24-86; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | NOT_FOUND | HIGH | Tidak diverifikasi apakah snow::HandshakeState/TransportState sendiri melakukan zeroize secara internal — di luar cakupan file ini. Import file hanya snow::{...} dan crate::error::Error (baris 18-19); tidak ada import/derive zeroize di manapun dalam file. |
| CB-053 | Klaim tekstualnya benar (NoiseKey dipakai untuk generate/secret_bytes/public_bytes di seluruh test), tapi rentang baris yang dikutip (104-104) hanya berisi baris import 'use crate::identity::keypair::NoiseKey;' — bukan pemanggilan method-nya. Pemanggilan .generate() ada di baris 133-134, 149-150, 176-177, 195-197 (termasuk eve_noise di baris 197 yang tidak disebut klaim asli); .secret_bytes() di baris 137, 142, 153, 157, 180, 184, 201, 206; .public_bytes() di baris 138, 154, 181, 190, 202. | src/crypto/handshake.rs:104-104; module=crypto_handshake; symbol=crate::identity; evidence=Verified JSON module=crypto_handshake; verdict=CORRECTED. | IMPLEMENTED | MEDIUM | Diverifikasi via grep — bukti aktual tersebar di 4 fungsi test, bukan satu baris tunggal. |
| CB-054 | testing: handshake_ik_roundtrip memverifikasi handshake IK 2-pesan lengkap, dengan assert is_finished()==true pada kedua sisi di dalam do_handshake(). | src/crypto/handshake.rs:131-145; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-055 | testing: transport_encrypt_decrypt_roundtrip memverifikasi encrypt() oleh alice lalu decrypt() oleh bob menghasilkan kembali plaintext byte-per-byte, assert_eq di baris 171. | src/crypto/handshake.rs:147-172; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-056 | testing: responder_verifies_initiator_identity memverifikasi bob.remote_static() == alice_noise.public_bytes(). | src/crypto/handshake.rs:174-191; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | NEEDS_CONFIRMATION | HIGH | Fakta yang diverifikasi: tidak ada pemanggilan alice.remote_static() di seluruh file test. API remote_static() sendiri generik dan bisa dipanggil kedua sisi (baris 77-79). |
| CB-057 | testing: wrong_peer_key_fails_handshake men-assert result.is_err() pada baris 216 ketika initiator pakai public key peer yang salah. | src/crypto/handshake.rs:193-217; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-058 | limitation: MAX_MSG_LEN=65535 hanya konstanta modul test, tidak ada validasi/pemaksaan batas di kode produksi handshake.rs. | src/crypto/handshake.rs:107-107; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | NEEDS_CONFIRMATION | MEDIUM | Baris 107 berada di dalam mod tests (#[cfg(test)] dimulai baris 101); tidak ada konstanta serupa di area produksi (baris 1-99). |
| CB-059 | comm_model: seluruh test mengirim handshake message dengan payload aplikasi kosong (&[]) pada write_message baris 115 dan 122; 0-RTT payload tidak diuji. | src/crypto/handshake.rs:115-122; module=crypto_handshake; symbol=handshake module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | NEEDS_CONFIRMATION | MEDIUM | Payload &[] juga dipakai di write_message baris 210 (test wrong_peer_key_fails_handshake) — konsisten, tidak ada satupun test yang mengirim payload aplikasi non-kosong. |
| CB-060 | architecture: src/crypto/mod.rs hanya mendeklarasikan pub mod handshake; tidak ada submodul crypto lain. | src/crypto/mod.rs:1-1; module=crypto_handshake; symbol=mod module; evidence=Verified JSON module=crypto_handshake; verdict=CONFIRMED. | IMPLEMENTED | HIGH | File mod.rs seluruhnya hanya 1 baris: 'pub mod handshake;'. |
| CB-061 | IdentityKey wraps an Ed25519 keypair (VerifyingKey + SigningKey) documented as the long-term identity, with #[derive(ZeroizeOnDrop)] and the verifying key excluded from zeroization via #[zeroize(skip)]. | src/identity/keypair.rs:6-15; module=identity; symbol=keypair module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-062 | NoiseKey wraps an X25519 keypair used for the Noise_IK DH handshake; kept as a separate struct from IdentityKey because Ed25519 and X25519 use different scalar spaces (per code comment). | src/identity/keypair.rs:40-49; module=identity; symbol=from; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-063 | KeyBundle aggregates IdentityKey + NoiseKey as the complete per-user key material; KeyBundle::generate() builds a fresh bundle by calling IdentityKey::generate() and NoiseKey::generate(). Struct also derives ZeroizeOnDrop. | src/identity/keypair.rs:74-89; module=identity; symbol=KeyBundle::generate; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-064 | The identity module consists of exactly two public submodules: keypair and vault (no other submodules declared). | src/identity/mod.rs:1-2; module=identity; symbol=mod module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH | mod.rs is exactly these 2 lines, nothing else. |
| CB-065 | IdentityKey::generate() creates a fresh Ed25519 keypair via SigningKey::generate(&mut OsRng), i.e. CSPRNG-based key generation. | src/identity/keypair.rs:18-22; module=identity; symbol=IdentityKey::generate; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-066 | IdentityKey::from_secret_bytes(bytes: [u8;32]) reconstructs the signing key from a raw 32-byte secret and re-derives the verifying key. | src/identity/keypair.rs:24-28; module=identity; symbol=IdentityKey::from_secret_bytes; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-067 | Raw Ed25519 secret bytes are exposed only through a pub(crate)-scoped secret_bytes() function, restricting export of the secret outside the identity module. | src/identity/keypair.rs:30-33; module=identity; symbol=keypair module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-068 | NoiseKey::generate() creates a fresh X25519 keypair via X25519Secret::random_from_rng(OsRng), CSPRNG-based. | src/identity/keypair.rs:51-56; module=identity; symbol=NoiseKey::generate; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Line 51 is the 'impl NoiseKey {' opener; the generate() body proper is lines 52-56. Minor, not a factual error — content matches. |
| CB-069 | Raw X25519 secret bytes are exposed only through pub(crate) secret_bytes(); its doc comment says the purpose is vault serialization and the snow (Noise) builder, but no snow/handshake usage of this function is present anywhere in these three files — only the vault serialization use (vault.rs) is actually observable here. | src/identity/keypair.rs:64-67; module=identity; symbol=keypair module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | PARTIAL | MEDIUM | The 'snow builder' part of the doc comment is unverified within the reviewed files. Confirmed via grep: 'snow' appears only in this doc comment across the three files; no other snow/sign/verify usage found. |
| CB-070 | IdentityKey, NoiseKey and KeyBundle all derive ZeroizeOnDrop so secret fields are wiped from memory on drop; the public/verifying fields are explicitly excluded from zeroization via #[zeroize(skip)] since they are not secret. | src/identity/keypair.rs:10-76; module=identity; symbol=keypair module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-071 | vault.rs documents a fixed 108-byte on-disk vault layout: 16-byte Argon2id salt + 12-byte ChaCha20 nonce + 64-byte ciphertext (32B Ed25519 sk + 32B X25519 sk) + 16-byte Poly1305 tag, with explicitly no header, magic bytes, version, or app-identifying string by design. | src/identity/vault.rs:1-31; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-072 | seal() assembles the on-disk vault by concatenating salt \|\| nonce \|\| (ciphertext+tag) into a fixed [u8; VAULT_SIZE] array. | src/identity/vault.rs:87-90; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-073 | unseal() parses the vault by splitting at fixed offsets (SALT_LEN, then NONCE_LEN) and treats the remainder as ciphertext+tag. | src/identity/vault.rs:99-101; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-074 | write_vault()/read_vault() perform plain std::fs::write / std::fs::read; no explicit file-permission hardening (e.g. restricting access mode) is present in this file. | src/identity/vault.rs:132-142; module=identity; symbol=std::fs; evidence=Verified JSON module=identity; verdict=CONFIRMED. | NOT_FOUND | MEDIUM | Absence claim limited to what's visible in vault.rs; permission hardening could theoretically exist elsewhere in the crate, not confirmed either way here. |
| CB-075 | seal(bundle, passphrase) flow: build 64-byte plaintext (Ed25519 sk \|\| X25519 sk) -> generate random 16-byte salt -> derive 256-bit key via Argon2id -> generate random 96-bit nonce -> ChaCha20Poly1305 encrypt -> concatenate salt\|\|nonce\|\|ciphertext+tag into VAULT_SIZE array. | src/identity/vault.rs:58-93; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-076 | unseal(vault, passphrase) flow: split stored salt/nonce/ciphertext -> re-derive key via Argon2id with stored salt -> ChaCha20Poly1305 decrypt+verify tag -> validate plaintext length equals 64 -> split back into 32-byte Ed25519 and X25519 secrets -> reconstruct KeyBundle. | src/identity/vault.rs:95-128; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-077 | derive_key() maps any Argon2 hash_password_into failure to Error::KeyDerivation. | src/identity/vault.rs:49-56; module=identity; symbol=Error::KeyDerivation; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-078 | seal() maps ChaCha20Poly1305 encryption failure to Error::Encryption. | src/identity/vault.rs:78-81; module=identity; symbol=Error::Encryption; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-079 | unseal() maps AEAD decrypt/tag-verification failure to Error::Decryption; the preceding code comment states this ambiguity between 'wrong passphrase' and 'vault corrupt' is intentional to prevent an oracle attack. | src/identity/vault.rs:95-112; module=identity; symbol=Error::Decryption; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-080 | unseal() also returns Error::Decryption if the decrypted plaintext length does not equal the expected 64 bytes (PLAINTEXT_LEN). | src/identity/vault.rs:114-116; module=identity; symbol=Error::Decryption; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-081 | read_vault() maps a wrong-sized file (try_into::<[u8;VAULT_SIZE]>() failure) to Error::Decryption rather than a distinct format/size error, per an inline comment stating this keeps the error 'ambiguous'. | src/identity/vault.rs:137-142; module=identity; symbol=Error::Decryption; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-082 | write_vault() and read_vault() map filesystem I/O failures to Error::Io. | src/identity/vault.rs:132-139; module=identity; symbol=Error::Io; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-083 | No key-rotation mechanism exists in these files, either for the passphrase-derived vault encryption key or for the long-term Ed25519/X25519 identity keys — every seal() call re-derives a key from a freshly random salt, but there is no scheduled/triggered rotation logic. | src/identity/vault.rs:39-93; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | NOT_FOUND | HIGH |  |
| CB-084 | These three files (keypair.rs, vault.rs, mod.rs) contain no signing/verification function calls and no Noise_IK handshake logic — only Ed25519/X25519 key generation, in-memory storage, and vault-level AEAD encryption of the raw secrets are implemented here; the actual use of these keys in the Noise protocol or for message signing is out of scope of this module. | src/identity/keypair.rs:1-119; module=identity; symbol=keypair module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | NOT_FOUND | HIGH | Grep for sign(/verify(/Signature across all three files returned no matches. |
| CB-085 | The vault format's deliberate lack of magic bytes/version field (justified in comments as 'SEC-05 compliance' / plausible deniability) means the code has no way to detect or migrate an old/incompatible vault layout if PLAINTEXT_LEN or field order ever changes. | src/identity/vault.rs:1-14; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | DOCUMENTED_ONLY | MEDIUM | The referenced 'SEC-05' requirement is not defined within these three files, so its exact scope/compliance cannot be independently confirmed from this review. |
| CB-086 | The ciphertext-length sanity check after encryption uses debug_assert_eq! rather than a runtime check, so it is compiled out entirely in release builds and provides no production safeguard. | src/identity/vault.rs:83-84; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Describes what the code does (debug-only assertion); the risk is that it offers no protection in release builds. |
| CB-087 | A code comment claims the chosen Argon2id parameters (19 MiB memory, t=2, p=1) take '~100ms' to unlock on 'modern hardware'. No benchmark, timing test, or measurement exists in these files to substantiate this figure. | src/identity/vault.rs:33-38; module=identity; symbol=vault module; evidence=Verified JSON module=identity; verdict=CONFIRMED. | DOCUMENTED_ONLY | LOW | Unverified comment claim; treat as documentation only, not a measured fact. |
| CB-088 | Test identity_key_roundtrip verifies that an Ed25519 IdentityKey survives a secret_bytes() -> from_secret_bytes() round trip with an identical resulting public key. | src/identity/keypair.rs:95-101; module=identity; symbol=identity_key_roundtrip; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-089 | Test noise_key_roundtrip verifies that an X25519 NoiseKey survives a secret_bytes() -> from_secret_bytes() round trip with an identical resulting public key. | src/identity/keypair.rs:103-109; module=identity; symbol=noise_key_roundtrip; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-090 | Test keybundle_keys_are_independent asserts that the Ed25519 identity public key bytes differ from the X25519 noise public key bytes within the same KeyBundle. | src/identity/keypair.rs:111-118; module=identity; symbol=keybundle_keys_are_independent; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-091 | Test seal_unseal_roundtrip verifies that seal() -> unseal() preserves both the Ed25519 and X25519 public keys, and that the sealed vault is exactly VAULT_SIZE (108) bytes. | src/identity/vault.rs:148-160; module=identity; symbol=seal_unseal_roundtrip; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-092 | Test wrong_passphrase_returns_error verifies that unseal() returns an Err when given an incorrect passphrase. | src/identity/vault.rs:162-167; module=identity; symbol=wrong_passphrase_returns_error; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-093 | Test vault_looks_random verifies that two seal() calls with the same bundle and passphrase produce different vault byte arrays (non-determinism from random salt/nonce). | src/identity/vault.rs:169-177; module=identity; symbol=vault_looks_random; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-094 | Test tampered_vault_returns_error verifies that flipping a single ciphertext byte causes unseal() to fail, demonstrating the AEAD tag detects tampering. | src/identity/vault.rs:179-186; module=identity; symbol=tampered_vault_returns_error; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-095 | Test vault_has_no_magic_bytes only checks that two seal() calls produce different first-16-byte (salt) prefixes; the test's own inline comment admits no stronger 'no magic bytes' property is actually asserted ('Tidak ada assertion spesifik yang bisa dibuat selain bukan constant'). | src/identity/vault.rs:188-198; module=identity; symbol=vault_has_no_magic_bytes; evidence=Verified JSON module=identity; verdict=CONFIRMED. | IMPLEMENTED | MEDIUM | Test name implies a broader guarantee than what is actually verified; the assertion is limited to salt randomness across two calls. |
| CB-096 | main.rs mendaftarkan 7 modul aplikasi: contacts, crypto, error, identity, session, transport, tui. | src/main.rs:1-7; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-097 | Parsing argumen CLI dilakukan manual (loop match atas std::env::args()) tanpa dependency clap, sesuai komentar eksplisit 'demi binary kecil'. | src/main.rs:23-109; module=main_and_error; symbol=std::env; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Komentar persis ada di baris 23 pada doc-comment struct Args; loop match manual ada di impl Args::parse() baris 54-108. |
| CB-098 | Mode default aplikasi adalah ONLINE (LAN+Tor); flag --offline mematikan Tor sehingga hanya LAN yang aktif. Flag --tor diterima tapi merupakan no-op karena online sudah default. | src/main.rs:86-87; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Lihat juga baris 31-32 (komentar field offline) dan baris 210 (online = !args.offline). Baris 86-87 persis '--offline => offline = true' dan '--tor => {} // no-op'. Baris 31 (bukan 31-32, field offline ada di 32) berisi komentar terkait, baris 210 'let online = !args.offline;' cocok. |
| CB-099 | --listen dan --dial saling eksklusif; keduanya diberikan bersamaan menghasilkan error string sebelum program lanjut. Bila tidak ada keduanya, mode ConnectKind::Auto dipakai. | src/main.rs:93-98; module=main_and_error; symbol=ConnectKind::Auto; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-100 | Lokasi data default adalah ~/.aksara (HOME di Unix, USERPROFILE di Windows, fallback '.'), vault identitas disimpan di ~/.aksara/id.key. | src/main.rs:36-51; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Urutan sebenarnya: HOME dicoba dulu, baru USERPROFILE (env::var_os('HOME').or_else(USERPROFILE)), tapi deskripsi 'HOME di Unix, USERPROFILE di Windows' tetap benar secara praktis karena Unix umumnya tak punya USERPROFILE dan Windows umumnya tak punya HOME. |
| CB-101 | Direktori cache & state Tor diturunkan dari nama file vault dengan pola '<stem>-tcache' dan '<stem>-tstate' di folder induk vault, supaya instance dengan vault berbeda tidak berbagi state Tor. | src/main.rs:135-151; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-102 | Pada subcommand `id`, bootstrap Tor dilakukan SINKRON (di-await langsung) sebelum invite code dicetak; jika gagal, error ditampilkan lewat Display ({e}) ke stderr dan invite tetap dicetak sebagai LAN-only (onion = None). | src/main.rs:223-236; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-103 | Pada mode TUI normal (bukan subcommand id), bootstrap Tor dijalankan di background lewat tokio::spawn dan hasilnya (Result<Arc<TorContext>, String>) dikirim ke TUI lewat mpsc::unbounded_channel agar startup TUI tidak terblokir; error TorContext::launch dikonversi jadi String polos via .to_string() sebelum masuk channel. | src/main.rs:287-301; module=main_and_error; symbol=tokio::spawn; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-104 | Unlock/pembuatan vault pada jalur run normal (bukan subcommand id) tidak terjadi di main.rs — main.rs hanya menghitung vault_exists (path.exists()) lalu meneruskannya bersama vault_path ke tui::run(); logika unlock aktual ada di modul tui (di luar cakupan file ini). | src/main.rs:303-312; module=main_and_error; symbol=tui::run; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | PARTIAL | MEDIUM | Untuk subcommand `id`, unlock terjadi langsung via load_or_create_vault() di baris 237. Catatan tambahan klaim tentang line 237 (subcommand id) juga benar: load_or_create_vault() dipanggil persis di baris 237. |
| CB-105 | rustls diberi default CryptoProvider berbasis ring lewat rustls::crypto::ring::default_provider().install_default(), dipanggil idempotent (Result diabaikan via `let _ =`) sebelum bootstrap Tor, supaya arti-client punya provider TLS. | src/main.rs:213-215; module=main_and_error; symbol=rustls::crypto; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-106 | Saat mode online, main.rs mengeset env var FS_MISTRUST_DISABLE_PERMISSIONS_CHECKS=true (jika belum diset) untuk melewati pengecekan permission filesystem fs-mistrust milik arti/Tor. Klaim asli menyebut ini terjadi 'di Windows' — tapi kode di baris 216-219 TIDAK punya guard platform apa pun (dikonfirmasi: tidak ada `cfg(windows)`/`target_os` di seluruh main.rs). Baris ini dieksekusi tanpa syarat OS setiap kali online=true, jadi env var ini di-set di SEMUA platform (Linux/macOS/Windows), bukan cuma Windows — hanya komentar kode ('Dev convenience di Windows') yang menyiratkan itu niatnya Windows-only, padahal implementasinya lebih luas. Ini melemahkan pengecekan permission fs-mistrust di semua OS, bukan hanya Windows. | src/main.rs:216-219; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CORRECTED. | IMPLEMENTED | HIGH | Verifikasi via grep 'cfg\(\|target_os\|windows' di src/main.rs -> No files found, mengkonfirmasi tidak ada gating platform sama sekali. |
| CB-107 | main() membuat tokio multi-thread runtime (enable_all()); kegagalan parse argumen exit code 2, kegagalan build runtime atau error dari real_main() exit code 1 dengan pesan ke stderr. | src/main.rs:315-339; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-108 | Passphrase vault dibaca dari environment variable AKSARA_PASSPHRASE bila ada (dipakai langsung tanpa prompt), atau interaktif dari stdin bila tidak ada. | src/main.rs:155-164; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-109 | Komentar kode eksplisit menyatakan input passphrase dari stdin MASIH TER-ECHO ke layar pada versi ini ('Catatan M1'); input tersembunyi via crossterm direncanakan menyusul di milestone M4 — belum diimplementasikan di file ini. | src/main.rs:153-154; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | PLANNED | HIGH |  |
| CB-110 | Bila passphrase diberikan lewat AKSARA_PASSPHRASE, nilainya berada di environment variable proses — tidak ada mekanisme di main.rs untuk membersihkan/menyembunyikannya dari environment setelah dibaca. | src/main.rs:156-158; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | MEDIUM | Fakta keberadaan jalur ini pasti (HIGH); dampak keamanan (visibilitas antar-proses) bergantung OS dan tidak diverifikasi lebih lanjut dari file ini, jadi confidence keseluruhan diturunkan ke MEDIUM. |
| CB-111 | Saat membuat vault baru, passphrase kosong ditolak dan menghasilkan Error::KeyDerivation sebelum KeyBundle::generate() dipanggil. | src/main.rs:175-178; module=main_and_error; symbol=Error::KeyDerivation; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-112 | KeyBundle memiliki field `identity` (diakses via .public_key().to_bytes()) dan `noise` (diakses via .public_bytes()/.secret_bytes()) — mengindikasikan dua pasangan kunci terpisah: identitas dan Noise. Implementasi struct KeyBundle sendiri ada di src/identity/keypair.rs, di luar cakupan file yang dibaca untuk tugas ini. | src/main.rs:187-200; module=main_and_error; symbol=KeyBundle; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | PARTIAL | MEDIUM |  |
| CB-113 | Fingerprint dihitung dari gabungan byte public key identitas dan public key noise via contacts::fingerprint(&ed_pub, &noise_pub); algoritma hash yang dipakai tidak terlihat di main.rs/error.rs (implementasi ada di modul contacts). | src/main.rs:191-191; module=main_and_error; symbol=contacts::fingerprint; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | NEEDS_CONFIRMATION | LOW |  |
| CB-114 | Invite code dibuat dari public key identitas, public key noise, dan opsional alamat onion via contacts::encode_invite(); format encoding tidak terlihat di file ini. | src/main.rs:192-192; module=main_and_error; symbol=contacts::encode_invite; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | NEEDS_CONFIRMATION | LOW |  |
| CB-115 | Pada output subcommand `id`, fingerprint ditampilkan dalam kelompok 8-karakter dipisah ' · ' (chunks(8) atas &str fingerprint), dan status Transport ditampilkan 'LAN + Tor' jika onion address berhasil didapat atau 'LAN' saja jika tidak. | src/main.rs:239-261; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-116 | Kontak yang dipra-muat lewat --add tanpa --name diberi nickname default berformat 'peer-<8 karakter pertama fingerprint>'. | src/main.rs:270-273; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-117 | Decode invite code dari --add yang gagal tidak menghentikan program — hanya mencetak peringatan ke stderr dan kontak tersebut dilewati (bukan fatal error). | src/main.rs:281-284; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-118 | Kontak dari CLI (--add) diproses (decode invite, dimasukkan ke contact_list) sebelum vault dibuka/di-unlock — tidak memerlukan identitas lokal ter-autentikasi terlebih dulu. | src/main.rs:265-285; module=main_and_error; symbol=main module; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-119 | enum Error (src/error.rs) memiliki 12 varian via #[derive(thiserror::Error)]: KeyDerivation, Encryption, Decryption, Noise(#[from] snow::Error), Io(#[from] std::io::Error), InvalidKey, FrameTooLarge, ConnectionClosed, Mdns(String), Tor(String), InvalidInvite, IdentityMismatch. | src/error.rs:4-43; module=main_and_error; symbol=thiserror::Error; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Dihitung ulang persis 12 varian dalam urutan yang sama seperti klaim. |
| CB-120 | std::io::Error dan snow::Error dikonversi otomatis ke Error via #[from], sehingga operator `?` di modul lain langsung men-propagate keduanya sebagai Error::Io / Error::Noise. read_passphrase() di main.rs memanfaatkan konversi Io ini (io::stdin().read_line(&mut s)?). | src/error.rs:16-20; module=main_and_error; symbol=std::io; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Baris rujukan main.rs 162 (io::stdin().read_line(&mut s)?) juga terkonfirmasi. |
| CB-121 | Error::Decryption sengaja diberi pesan Display yang ambigu ('vault could not be opened') — komentar kode di puncak file (baris 1-3) dan di atas varian (baris 12-13) menyatakan ini disengaja supaya penyerang tidak bisa membedakan passphrase salah vs vault corrupt lewat pesan error (mitigasi oracle attack pada pesan error, bukan klaim tentang timing/side-channel). | src/error.rs:1-14; module=main_and_error; symbol=Error::Decryption; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Komentar atas varian sebenarnya persis di baris 12 (satu baris, bukan 12-13); baris 13 adalah atribut #[error(...)] dan baris 14 nama varian Decryption. Perbedaan ini kosmetik, tidak mengubah substansi klaim. |
| CB-122 | Pesan Display sebagian varian menyertakan detail internal via {0} (Io: 'I/O error: {0}', Mdns: 'mDNS error: {0}', Tor: 'Tor error: {0}'), sedangkan varian lain yang berkaitan keamanan (KeyDerivation, Encryption, Decryption, Noise, InvalidKey, FrameTooLarge, ConnectionClosed, InvalidInvite, IdentityMismatch) memakai string tetap tanpa {0} sehingga tidak membocorkan detail internal ke Display walau untuk Noise sumber errornya (snow::Error) tetap disimpan lewat #[from] untuk keperluan std::error::Error::source(). | src/error.rs:6-42; module=main_and_error; symbol=snow::Error; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-123 | Error::IdentityMismatch dikomentari sebagai 'Remote static key tidak cocok dengan kontak yang diharapkan — fail closed'; menandakan ada verifikasi kunci statis peer terhadap daftar kontak di suatu tempat dalam sistem (implementasi pemakaiannya tidak terlihat di error.rs/main.rs, hanya deklarasi varian). | src/error.rs:40-42; module=main_and_error; symbol=Error::IdentityMismatch; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | NEEDS_CONFIRMATION | MEDIUM | Keberadaan varian dan komentar desainnya pasti (HIGH); tempat/logika pemakaiannya di modul lain tidak diverifikasi di sini. |
| CB-124 | Varian Error::FrameTooLarge menunjukkan ada batas ukuran maksimum frame protokol, tapi nilai batas pastinya (angka byte) tidak dideklarasikan di error.rs maupun main.rs — kemungkinan didefinisikan sebagai const di modul transport/session yang tidak dibaca untuk tugas ini. | src/error.rs:25-26; module=main_and_error; symbol=Error::FrameTooLarge; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | NEEDS_CONFIRMATION | LOW |  |
| CB-125 | Tidak ditemukan fungsi #[test] maupun blok #[cfg(test)] pada src/main.rs (339 baris) atau src/error.rs (43 baris) — tidak ada unit test langsung di dua file ini. | src/main.rs:1-339; module=main_and_error; symbol=maupun; evidence=Verified JSON module=main_and_error; verdict=CONFIRMED. | NOT_FOUND | HIGH | Panjang file dikonfirmasi: main.rs 339 baris, error.rs 43 baris, tanpa #[test]/#[cfg(test)] pada keduanya. |
| CB-126 | SessionState memodelkan siklus hidup room sebagai enum 4 varian: Connecting, Handshaking, Active, Closed. | src/session/mod.rs:19-25; module=session; symbol=SessionState; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-127 | SessionState::Connecting dideklarasikan tapi tidak pernah dikirim sebagai event dari dalam run_session pada file ini. | src/session/mod.rs:21-21; module=session; symbol=SessionState::Connecting; evidence=Verified JSON module=session; verdict=CONFIRMED. | NEEDS_CONFIRMATION | MEDIUM | Kemungkinan Connecting di-set oleh caller sebelum memanggil run_session; caller tidak dibaca dalam audit ini. Bukti event yang benar-benar dikirim: baris 107 (Handshaking), 161 (Active), 277 (Closed). Grep 'Connecting' di seluruh file hanya menemukan baris 3 (doc comment lifecycle) dan baris 21 (deklarasi varian) — tidak pernah dipakai dalam events.send()... |
| CB-128 | SessionEvent::Error dideklarasikan untuk 'error fatal' tapi tidak pernah dikonstruksi/dikirim di dalam run_session; kegagalan dikembalikan lewat Result<(), Error>. | src/session/mod.rs:82-83; module=session; symbol=SessionEvent::Error; evidence=Verified JSON module=session; verdict=CONFIRMED. | NEEDS_CONFIRMATION | HIGH | Fakta 'tidak pernah dikirim dalam file ini' pasti benar (HIGH); apakah caller menerjemahkan Err menjadi SessionEvent::Error tidak dapat dipastikan tanpa membaca caller. Grep 'SessionEvent::Error' di seluruh file hanya muncul sekali, di baris 442 dalam test (memastikan varian ini TIDAK diterima, bukan mengirimkannya). Tidak ada events.send(SessionEvent::Er... |
| CB-129 | Pembacaan frame dipindahkan ke tokio::spawn task terpisah, dikomunikasikan lewat channel RxFrame, karena read_frame tidak cancel-safe di dalam tokio::select!. | src/session/mod.rs:166-190; module=session; symbol=reader task / RxFrame; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-130 | tokio::io::split(stream) dipakai untuk handle baca/tulis independen, generik untuk TcpStream maupun arti DataStream lewat bound generic S. | src/session/mod.rs:111-111; module=session; symbol=tokio::io; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Baris 111 berisi pemanggilan split; klaim 'generik TcpStream/arti DataStream' didukung komentar baris 109-110 dan bound generic S pada signature (baris 105). |
| CB-131 | Loop aktif (baris 198-271, `loop { tokio::select! { ... } }`) memang memakai 3 cabang: outgoing.recv() (200), keepalive.tick() (232), frames.recv() (243) — bagian ini akurat. Tapi komentar yang menegaskan 'semua arm select di sini cancel-safe (`recv`, `tick`)' berada di baris 175, SEBELUM loop dimulai (dalam blok komentar baris 167-175 yang menjelaskan kenapa read_frame dipindah ke task terpisah) — bukan di dalam rentang baris 198-271 yang dikutip sebagai evidence. | src/session/mod.rs:198-271; module=session; symbol=tokio::select; evidence=Verified JSON module=session; verdict=CORRECTED. | IMPLEMENTED | HIGH | Struktur 3-cabang select benar dan lengkap di 198-271; hanya rujukan baris untuk 'komentar cancel-safe' yang meleset satu blok ke atas (baris 172-175, bukan di dalam 198-271). |
| CB-132 | SessionCmd::Text diserialisasi sebagai 1 byte tag TYPE_TEXT (0x00) diikuti byte UTF-8 teks, sebelum dienkripsi. | src/session/mod.rs:203-209; module=session; symbol=SessionCmd::Text; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-133 | SessionCmd::Blur(on) diserialisasi sebagai persis 2 byte: [TYPE_BLUR(0x01), on as u8]. | src/session/mod.rs:210-210; module=session; symbol=SessionCmd::Blur; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-134 | Keepalive ping adalah payload plaintext 1 byte [TYPE_PING] yang dienkripsi & dikirim tiap tick; penerima mencocokkan TYPE_PING dan mengabaikannya (tanpa balasan otomatis, tanpa diteruskan ke UI). | src/session/mod.rs:232-264; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-135 | Setelah dekripsi berhasil, dispatch berdasarkan byte pertama: TYPE_TEXT -> Message via from_utf8_lossy; TYPE_BLUR hanya jika panjang >=2 -> PeerBlur(pt[1]==0x01); byte tak dikenal diabaikan tanpa fatal. | src/session/mod.rs:256-266; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-136 | Format payload plaintext: 1 byte tag di depan lalu body. Tiga tag: TYPE_TEXT=0x00, TYPE_BLUR=0x01, TYPE_PING=0x02. | src/session/mod.rs:36-41; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-137 | Framing pada kabel (MAX_FRAME_LEN dsb) didelegasikan ke crate::transport::frame, diimpor tapi tidak didefinisikan di file ini. | src/session/mod.rs:15-15; module=session; symbol=crate::transport; evidence=Verified JSON module=session; verdict=CONFIRMED. | NEEDS_CONFIRMATION | LOW | Perlu baca src/transport/frame.rs untuk memastikan nilai MAX_FRAME_LEN — di luar cakupan file yang diaudit. |
| CB-138 | Komentar & test menyatakan batas pesan Noise 65535 byte; kegagalan encrypt akibat itu ditangani sebagai SessionEvent::Notice non-fatal, sesi tetap hidup. | src/session/mod.rs:212-224; module=session; symbol=SessionEvent::Notice; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Dikonfirmasi lewat test oversized_message_does_not_kill_session, baris 409-457 (payload 70.000 byte). |
| CB-139 | run_session menerima local_noise_sk: [u8; 32] dan peer_noise_pk: Option<[u8; 32]> — kunci statis Noise 256-bit sebagai array byte mentah. | src/session/mod.rs:96-97; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-140 | Untuk Role::Initiator, peer_noise_pk wajib Some; jika None -> Err(Error::InvalidKey) sebelum handshake dimulai. | src/session/mod.rs:119-119; module=session; symbol=Role::Initiator; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-141 | Untuk Role::Responder, jika peer_noise_pk berisi nilai, dibandingkan dengan hs.remote_static(); ketidakcocokan/absen -> Err(Error::IdentityMismatch), 'fail closed' secara eksplisit di komentar. | src/session/mod.rs:145-151; module=session; symbol=Role::Responder; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Dikonfirmasi test responder_rejects_unknown_peer, baris 502-553. |
| CB-142 | Jika responder memanggil run_session dengan peer_noise_pk=None, tidak ada pengecekan identitas peer sama sekali di jalur kode ini. | src/session/mod.rs:145-151; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Dikonfirmasi test responder_rejects_unknown_peer, baris 502-553. Blok `if let Some(expected) = peer_noise_pk` sepenuhnya dilewati saat None; tidak ada jalur verifikasi lain di file ini. |
| CB-143 | Baris 88-90 hanya menyatakan: 'peer_noise_pk untuk initiator WAJIB (static key responder yang dituju). Untuk responder, jika Some, dipakai memverifikasi bahwa remote static key cocok dengan kontak yang diharapkan (fail closed bila tidak cocok).' Frasa 'pola IK mengasumsikan initiator sudah tahu kunci publik responder' dan 'responder baru mempelajari static key initiator selama handshake' TIDAK tertulis literal di baris ini — itu adalah pengetahuan umum protokol Noise_IK yang ditambahkan auditor sebagai penjelasan, bukan isi komentar file session/mod.rs. | src/session/mod.rs:88-90; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CORRECTED. | DOCUMENTED_ONLY | MEDIUM | Interpretasi properti Noise_IK ini berasal dari komentar dokumentasi fungsi, bukan diverifikasi lewat pembacaan modul crypto::handshake. Substansi (peer_noise_pk wajib utk initiator, dipakai verifikasi opsional di responder) akurat, tapi rasionalisasi protokol Noise_IK di baliknya adalah interpretasi auditor, bukan kutipan dari file. |
| CB-144 | KEEPALIVE_INTERVAL 30 detik (non-test), 5 milidetik (#[cfg(test)]); komentar menjelaskan sengaja tidak memakai start_paused karena clock palsu tidak deterministik antar platform. | src/session/mod.rs:59-68; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-145 | Doc-comment test keepalive_ping_is_not_delivered_as_message menyatakan 'KEEPALIVE_INTERVAL sudah 50 ms di build test', padahal konstanta sebenarnya 5 ms — komentar dan kode tidak konsisten. | src/session/mod.rs:68-68; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | INCONSISTENT | HIGH | Bandingkan baris 68 (const, 5 ms) dengan baris 460-461 (doc-comment test, mengklaim 50 ms). Tidak memengaruhi kebenaran test karena test memakai konstanta KEEPALIVE_INTERVAL langsung (baris 488), tapi dokumentasi menyesatkan. Baris 68: from_millis(5). Doc comment di baris 460 memang literal berbunyi '`KEEPALIVE_INTERVAL` sudah 50 ms di build test' — inkon... |
| CB-146 | Tick pertama interval yang selesai seketika sengaja 'dihabiskan' lewat keepalive.tick().await sebelum loop utama; MissedTickBehavior diset Delay. | src/session/mod.rs:192-196; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-147 | Kegagalan session.encrypt() saat kirim pesan chat tidak mematikan sesi (Notice + continue); hanya kegagalan I/O write_frame setelah enkripsi berhasil yang break loop. | src/session/mod.rs:212-227; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-148 | Kegagalan session.decrypt() pada frame masuk langsung break ('fail closed'); sesudahnya tetap kirim StateChanged(Closed) dan return Ok(()), bukan Err, tanpa event Error terpisah. | src/session/mod.rs:253-279; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH | UI tidak dapat membedakan 'sesi ditutup karena dekripsi gagal (potensi gangguan/tampering)' dari penutupan lain lewat channel events saja, selain event PeerLeft yang khusus untuk EOF bersih (baris 246-249). |
| CB-149 | Deteksi koneksi mati selama sesi aktif sepenuhnya bergantung pada kegagalan write_frame, bukan ping tak terbalas — dinyatakan eksplisit sebagai keterbatasan disengaja (komentar ponytail). | src/session/mod.rs:54-58; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | DOCUMENTED_ONLY | HIGH | Komentar menyebut upgrade path bila dibutuhkan: batas 'tak ada frame masuk selama N detik' di sisi reader — belum diimplementasikan di file ini. |
| CB-150 | reader.abort() dipanggil eksplisit setelah loop select selesai, karena drop channel saja tidak membangunkan task pembaca yang menunggu I/O dari peer diam. | src/session/mod.rs:273-275; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-151 | File ini tidak mengimpor/memakai crate logging (log::/tracing::); seluruh pelaporan status ke UI lewat channel SessionEvent. | src/session/mod.rs:10-16; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | NOT_FOUND | HIGH | Grep 'log::' dan 'tracing::' di seluruh file: nihil hasil. |
| CB-152 | Buffer `pt` (hasil decrypt, baris 253: `let mut pt = vec![0u8; frame.len()];`) memang plain Vec<u8> tanpa zeroize, dan penyaluran ke UI di rentang 256-266 murni lewat events.send() in-memory — bagian ini akurat untuk rentang baris 253-266. Tapi buffer `payload` (untuk pesan KELUAR, dibangun via `Vec::with_capacity` dan `p.push`/`extend_from_slice`) sebenarnya berada di baris 205-211, DI LUAR rentang evidence 253-266 yang dikutip — bukan berarti klaimnya salah (payload juga plain Vec<u8> tanpa zeroize), hanya kutipan barisnya tidak mencakup lokasi variabel itu. | src/session/mod.rs:253-266; module=session; symbol=Vec::with_capacity; evidence=Verified JSON module=session; verdict=CORRECTED. | NOT_FOUND | HIGH | Tidak ditemukan pemanggilan zeroize di buffer plaintext lokal ini; setelah keluar scope, memori dilepas lewat drop biasa Rust (tidak dijamin di-overwrite nol). Grep 'zeroize' di seluruh file: nihil hasil, jadi kesimpulan 'tanpa zeroize' tetap benar untuk kedua buffer (payload maupun pt); hanya rentang baris yang dikutip tidak mencakup lokasi definisi `pay... |
| CB-153 | SessionEvent (StateChanged, Message, PeerBlur, PeerLeft, Notice, Error) adalah satu-satunya antarmuka keluar dari run_session ke UI lewat UnboundedSender<SessionEvent>. | src/session/mod.rs:70-84; module=session; symbol=mod module; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-154 | Test lan_session_message_roundtrip: integration test 2 task run_session di TCP loopback, handshake Noise_IK penuh, verifikasi pesan dua arah lewat SessionCmd::Text/SessionEvent::Message. | src/session/mod.rs:302-358; module=session; symbol=lan_session_message_roundtrip; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-155 | Test blur_toggle_roundtrip: verifikasi byte tag TYPE_BLUR roundtrip end-to-end — initiator SessionCmd::Blur(true), responder terima SessionEvent::PeerBlur(true). | src/session/mod.rs:363-404; module=session; symbol=blur_toggle_roundtrip; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-156 | Test oversized_message_does_not_kill_session: kirim 70.000 byte, verifikasi SessionEvent::Notice 'terlalu besar' (bukan Error), lalu pesan normal berikutnya tetap terkirim. | src/session/mod.rs:409-457; module=session; symbol=oversized_message_does_not_kill_session; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-157 | Test keepalive_ping_is_not_delivered_as_message: tunggu 3x KEEPALIVE_INTERVAL tanpa trafik, lalu kirim pesan asli; pesan pertama di event stream adalah pesan asli, bukan ping. | src/session/mod.rs:462-498; module=session; symbol=keepalive_ping_is_not_delivered_as_message; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-158 | Test responder_rejects_unknown_peer: responder diberi peer_noise_pk salah; run_session responder mengembalikan Err(Error::IdentityMismatch). | src/session/mod.rs:502-553; module=session; symbol=responder_rejects_unknown_peer; evidence=Verified JSON module=session; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-159 | Modul mDNS discovery: responder mengiklankan _aksara._tcp.local. dengan TXT fp=<hex>; initiator browse dan cocokkan fingerprint sebelum dial. | src/transport/lan.rs:1-21; module=transport_lan; symbol=lan module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-160 | Yang diiklankan via mDNS hanya fingerprint Ed25519; X25519 Noise key TIDAK diiklankan. | src/transport/lan.rs:9-11; module=transport_lan; symbol=lan module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Ini pernyataan negatif (apa yang TIDAK dikirim) — diverifikasi dari struct props di advertise() (baris 90) yang hanya berisi pasangan ("fp", fingerprint), tidak ada field kunci X25519. |
| CB-161 | Komentar modul mengakui metadata leak level LAN sebagai trade-off yang 'dapat diterima untuk M1', pengerasan menyusul. | src/transport/lan.rs:9-12; module=transport_lan; symbol=lan module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | DOCUMENTED_ONLY | HIGH |  |
| CB-162 | MAX_LABEL_LEN=32 dipilih sebagai margin aman terhadap batas 63 byte DNS label; komentar menyebut mdns-sd menegakkan lewat assert! internal yang panic bila terlampaui. | src/transport/lan.rs:23-27; module=transport_lan; symbol=lan module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | DOCUMENTED_ONLY | MEDIUM | Klaim perilaku internal mdns-sd (assert! yang panic) berasal dari komentar source project sendiri, bukan dari pembacaan langsung source crate mdns-sd 0.20.0 dalam audit ini — tidak diverifikasi independen. |
| CB-163 | fn safe_label memendekkan string ke ≤32 dengan iterasi per-char, berhenti sebelum memotong karakter UTF-8 multi-byte. | src/transport/lan.rs:33-42; module=transport_lan; symbol=safe_label; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-164 | fn is_lan_dialable menolak loopback, unspecified, link-local IPv4; seluruh IPv6 ditolak. | src/transport/lan.rs:50-55; module=transport_lan; symbol=is_lan_dialable; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-165 | fn local_lan_ips enumerasi via if_addrs::get_if_addrs, filter is_lan_dialable, gagal → Vec kosong via unwrap_or_default. | src/transport/lan.rs:58-68; module=transport_lan; symbol=if_addrs::get_if_addrs; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-166 | struct DiscoveredPeer { fingerprint: String, addr: SocketAddr }. | src/transport/lan.rs:71-74; module=transport_lan; symbol=DiscoveredPeer; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-167 | fn new_daemon() membuat ServiceDaemon baru, membungkus error ke Error::Mdns(e.to_string()). | src/transport/lan.rs:77-79; module=transport_lan; symbol=Error::Mdns; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-168 | fn advertise() membangun hostname/instance name via safe_label(fingerprint); fingerprint lengkap dibawa di TXT 'fp' untuk matching saat browse. | src/transport/lan.rs:82-90; module=transport_lan; symbol=advertise; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-169 | advertise() memilih IP LAN nyata via local_lan_ips() daripada enable_addr_auto(); fallback ke enable_addr_auto() bila enumerasi kosong/gagal. | src/transport/lan.rs:92-110; module=transport_lan; symbol=lan module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-170 | fn spawn_browse() menjalankan thread terpisah (blocking recv) agar tidak memblokir runtime async; berhenti saat tx ditutup atau receiver mDNS berhenti mengirim event. | src/transport/lan.rs:120-155; module=transport_lan; symbol=spawn_browse; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-171 | Hanya ServiceEvent::ServiceResolved ditangani; peer tanpa TXT 'fp' dilewati (continue). | src/transport/lan.rs:126-132; module=transport_lan; symbol=ServiceEvent::ServiceResolved; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-172 | Alamat peer difilter ulang dengan is_lan_dialable (defense-in-depth) sebelum dikirim sebagai DiscoveredPeer ke channel tx. | src/transport/lan.rs:133-149; module=transport_lan; symbol=lan module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-173 | Operasi mdns-sd fallible diseragamkan via .map_err(\|e\| Error::Mdns(e.to_string())), berulang di baris 78, 100, 109, 113, 123. | src/transport/lan.rs:78-123; module=transport_lan; symbol=Error::Mdns; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Pola berulang di baris 78, 100, 109, 113, 123. Diverifikasi via grep: pola persis muncul di kelima baris yang disebut (78, 100, 109, 113, 123). |
| CB-174 | #[test] safe_label_truncates_full_fingerprint_below_dns_limit: fingerprint 64-char dipendekkan ke ≤32 dan <64. | src/transport/lan.rs:161-168; module=transport_lan; symbol=safe_label_truncates_full_fingerprint_below_dns_limit; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-175 | #[test] safe_label_leaves_short_input_untouched: safe_label("abc123") == "abc123". | src/transport/lan.rs:170-173; module=transport_lan; symbol=safe_label_leaves_short_input_untouched; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-176 | #[test] lan_dialable_accepts_private_rejects_linklocal_and_loopback: terima IPv4 privat, tolak APIPA/loopback/unspecified/loopback IPv6. | src/transport/lan.rs:176-187; module=transport_lan; symbol=lan_dialable_accepts_private_rejects_linklocal_and_loopback; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-177 | #[test] safe_label_never_splits_multibyte_char: 17x 'é' (34 byte) dipotong tanpa memecah char, hasil ≤32 dan valid char boundary. | src/transport/lan.rs:190-197; module=transport_lan; symbol=safe_label_never_splits_multibyte_char; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-178 | Doc-comment modul transport: M1=LAN(mDNS+TCP), M2=Tor via arti-client, fallback LAN-first→Tor. | src/transport/mod.rs:1-4; module=transport_lan; symbol=mod module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | DOCUMENTED_ONLY | HIGH |  |
| CB-179 | const LAN_AUTO_TIMEOUT: Duration = 3 detik, batas percobaan LAN sebelum fallback Tor. | src/transport/mod.rs:24-25; module=transport_lan; symbol=LAN_AUTO_TIMEOUT; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-180 | enum Role { Initiator, Responder }; komentar: peran deterministik dari perbandingan fingerprint. | src/transport/mod.rs:27-33; module=transport_lan; symbol=Role; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Klaim tentang Noise_IK sendiri (algoritma handshake) tidak diverifikasi di 3 file ini — hanya disebut di komentar; implementasi Noise_IK berada di modul crypto yang tidak diaudit di sini. |
| CB-181 | fn role_from_fp: Role::Initiator jika my_fp < target_fp (operator < bawaan &str), selain itu Role::Responder. | src/transport/mod.rs:101-107; module=transport_lan; symbol=Role::Initiator; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Perbandingan memakai operator `<` bawaan &str (byte-wise), bukan perbandingan constant-time. Risiko timing side-channel dinilai rendah di sini karena kedua fingerprint yang dibandingkan bukan rahasia (sudah saling diketahui via kontak), tetapi ini bukan hal yang dipastikan aman secara umum — tidak ada penjelasan eksplisit di kode soal ini. |
| CB-182 | enum LanMode { Auto, Listen(u16), Dial(SocketAddr), Off }, Off ditandai #[allow(dead_code)] dengan catatan belum di-wire ke UI. | src/transport/mod.rs:35-48; module=transport_lan; symbol=LanMode; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-183 | enum Conn { Tcp(TcpStream), Tor(DataStream) } mendelegasikan poll_read/write/flush/shutdown; #[allow(clippy::large_enum_variant)] dengan alasan Conn dibuat sekali per sesi. | src/transport/mod.rs:50-99; module=transport_lan; symbol=clippy::large_enum_variant; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-184 | fn establish(): role dari LanMode/fingerprint; tor_available = tor.is_some() && onion.is_some(); LAN dicoba dulu, timeout LAN_AUTO_TIMEOUT hanya jika Auto dan Tor tersedia; LAN-only tunggu tanpa batas waktu. | src/transport/mod.rs:118-155; module=transport_lan; symbol=establish; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-185 | Jika LAN gagal dan Tor tidak tersedia, kembalikan error LAN asli; jika Tor tersedia, lanjut: Initiator→tor_dial_with_retry, Responder→accept_timeout(TOR_ACCEPT_TIMEOUT) atau Error::ConnectionClosed. | src/transport/mod.rs:148-171; module=transport_lan; symbol=Error::ConnectionClosed; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-186 | Klaim faktual (delay 8s, total timeout 120s, alasan propagasi descriptor) akurat, tapi evidence_line_start=109/end=197 SALAH sebagai satu blok kontinu: rentang itu justru mencakup seluruh fn establish() (baris 118-172) yang tidak terkait tor_dial_with_retry. Lokasi yang benar: const TOR_DIAL_TOTAL_TIMEOUT/TOR_DIAL_RETRY_DELAY/TOR_ACCEPT_TIMEOUT di baris 109-116; doc-comment + fn tor_dial_with_retry sendiri di baris 174-197 (deklarasi fn di baris 180, isi retry-loop baris 186-196). | src/transport/mod.rs:109-197; module=transport_lan; symbol=establish; evidence=Verified JSON module=transport_lan; verdict=CORRECTED. | IMPLEMENTED | HIGH | Isi klaim tidak fabrikasi -- hanya rentang baris yang diberikan tidak presisi/menyesatkan karena melompati fungsi lain yang tidak terkait. |
| CB-187 | fn try_lan mendispatch berdasarkan LanMode (Dial/Listen/Auto/Off), opsional dibungkus tokio::time::timeout yang dikonversi ke Error::ConnectionClosed. | src/transport/mod.rs:201-227; module=transport_lan; symbol=tokio::time; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-188 | lan_auto() jalur Responder: bind ephemeral port, advertise, accept satu koneksi, shutdown daemon (error diabaikan). | src/transport/mod.rs:229-239; module=transport_lan; symbol=mod module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-189 | lan_auto() jalur Initiator: browse, filter fingerprint, dedupe via HashSet<SocketAddr>, coba tiap alamat baru, lanjut ke berikutnya jika gagal (simpan last_err), berhenti sukses (shutdown daemon) atau channel tertutup. | src/transport/mod.rs:240-274; module=transport_lan; symbol=mod module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-190 | transport/mod.rs mereferensikan Error::Io (baris 210, 263), Error::Tor (baris 158, 161), Error::ConnectionClosed (baris 168, 217, 224, 269); transport/lan.rs mereferensikan Error::Mdns (baris 78, 100, 109, 113, 123). Error::FrameTooLarge TIDAK muncul sama sekali di mod.rs atau lan.rs -- varian ini hanya direferensikan di src/transport/frame.rs (baris 24, 51, 150). Definisi lengkap enum Error memang tidak berada di 3 file yang diaudit, itu bagian yang benar. | src/transport/mod.rs:21-21; module=transport_lan; symbol=Error::Io; evidence=Verified JSON module=transport_lan; verdict=CORRECTED. | NEEDS_CONFIRMATION | LOW | Diverifikasi via grep di kedua file: Error::FrameTooLarge nihil di mod.rs maupun lan.rs, hanya ada di frame.rs. Sisa varian yang disebut memang benar direferensikan (walau tidak semuanya di kedua file -- Mdns hanya di lan.rs, Io/Tor/ConnectionClosed hanya di mod.rs). |
| CB-191 | Format frame: [2-byte big-endian length][payload]; batas 2 byte=65535 sama dengan batas maksimum Noise message, 1 frame=1 Noise message tanpa fragmentasi. | src/transport/frame.rs:1-9; module=transport_lan; symbol=frame module; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | DOCUMENTED_ONLY | MEDIUM | Bagian yang diverifikasi langsung dari kode: const MAX_FRAME_LEN=65535 dan penegakannya oleh write_frame/read_frame (lihat claim terpisah). Klaim padanan 'frame = satu Noise message' adalah asersi komentar; frame.rs sendiri TIDAK melakukan enkripsi/dekripsi Noise apa pun — hanya framing byte mentah generik di atas AsyncRead/AsyncWrite. Verifikasi bahwa pa... |
| CB-192 | pub const MAX_FRAME_LEN: usize = 65535. | src/transport/frame.rs:16-16; module=transport_lan; symbol=MAX_FRAME_LEN; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-193 | async fn write_frame menolak payload>MAX_FRAME_LEN dengan Error::FrameTooLarge; jika lolos tulis 2-byte BE length lalu payload lalu flush(). | src/transport/frame.rs:19-31; module=transport_lan; symbol=Error::FrameTooLarge; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-194 | async fn read_frame: UnexpectedEof saat baca header→Ok(0); error IO lain→Error::Io; panjang > buf.len()→Error::FrameTooLarge sebelum baca payload; read_exact membaca persis len byte. | src/transport/frame.rs:37-56; module=transport_lan; symbol=Error::Io; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-195 | Komentar di atas test cancel-safety menyatakan read_frame TIDAK cancel-safe, alasan session::run_session wajib pakai task terpisah bukan arm select!. | src/transport/frame.rs:97-104; module=transport_lan; symbol=session::run_session; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | DOCUMENTED_ONLY | HIGH | Perilaku non-cancel-safe ini didemonstrasikan langsung oleh test di baris 105-141 (bukan sekadar diklaim di komentar) — lihat claim testing terkait. Klaim tentang session::run_session sendiri tidak diverifikasi karena file session.rs tidak termasuk dalam 3 file yang diaudit. |
| CB-196 | #[tokio::test] frame_roundtrip: write_frame lalu read_frame via tokio::io::duplex menghasilkan payload identik. | src/transport/frame.rs:62-72; module=transport_lan; symbol=tokio::test; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-197 | #[tokio::test] multiple_frames_in_order: dua write_frame berurutan dibaca kembali dua kali berurutan, batas payload benar. | src/transport/frame.rs:74-86; module=transport_lan; symbol=tokio::test; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-198 | #[tokio::test] eof_returns_zero: penulis di-drop, read_frame pembaca mengembalikan Ok(0). | src/transport/frame.rs:88-95; module=transport_lan; symbol=tokio::test; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-199 | #[tokio::test] read_frame_yang_dibatalkan_merusak_sinkronisasi_stream: simulasi pembatalan via timeout 50ms saat separuh payload 300-byte terkirim, membuktikan stream desync/rusak. | src/transport/frame.rs:105-141; module=transport_lan; symbol=tokio::test; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-200 | #[tokio::test] oversized_payload_rejected: write_frame dengan payload MAX_FRAME_LEN+1 byte mengembalikan Err(Error::FrameTooLarge). | src/transport/frame.rs:143-151; module=transport_lan; symbol=tokio::test; evidence=Verified JSON module=transport_lan; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-201 | TorContext menyatukan client arti-client yang sudah bootstrap, handle onion service yang berjalan (_service), alamat .onion sendiri, dan channel penerima stream masuk (incoming) dalam satu struct. | src/transport/tor.rs:32-40; module=transport_tor; symbol=TorContext; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Struct field persis: client: Arc<TorClient<PreferredRuntime>>, _service: Arc<RunningOnionService>, pub onion_address: String, incoming: Mutex<mpsc::UnboundedReceiver<DataStream>>. |
| CB-202 | Doc-comment modul menyatakan Tor transport dipakai sebagai layer defense-in-depth: Tor menyembunyikan lokasi jaringan, sedangkan Noise_IK (di layer atas, di luar file ini) yang mengamankan konten dan autentikasi identitas. | src/transport/tor.rs:4-7; module=transport_tor; symbol=tor module; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | DOCUMENTED_ONLY | MEDIUM | Ini klaim dari komentar dokumentasi, bukan kode yang dapat diverifikasi di file ini sendiri — pembungkusan Noise_IK terjadi di modul lain yang tidak dibaca dalam audit ini. Teks asli baris 6-7: '(defense-in-depth: Tor menyembunyikan jaringan, Noise mengamankan konten + autentikasi identitas)'. Kata 'lokasi' di klaim adalah parafrase interpretatif, bukan k... |
| CB-203 | TOR_VIRTUAL_PORT bernilai 9999, dipakai sebagai nomor port virtual protokol saat initiator connect ke onion peer; bukan port OS nyata yang di-bind. | src/transport/tor.rs:27-29; module=transport_tor; symbol=TOR_VIRTUAL_PORT; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | pub const TOR_VIRTUAL_PORT: u16 = 9999; dengan doc-comment persis seperti diklaim. |
| CB-204 | launch() mengonfigurasi TorClientConfig dengan cache_dir dan state_dir terpisah (dibungkus CfgPath) yang diberikan caller, sebelum client di-build dan di-bootstrap. | src/transport/tor.rs:51-56; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | builder.storage().cache_dir(CfgPath::new(...)).state_dir(CfgPath::new(...)) lalu builder.build() -> config, semuanya sebelum TorClient::create_bootstrapped(config) di baris 58-60. |
| CB-205 | Komentar menyatakan state_dir menyimpan key onion persisten sehingga alamat .onion stabil antar-run, namun mekanisme penyimpanan/rotasi key onion itu sendiri berada di dalam crate arti-client/tor-hsservice dan tidak terlihat di file ini. | src/transport/tor.rs:3-4; module=transport_tor; symbol=tor module; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | NEEDS_CONFIRMATION | LOW | Tidak ada kode di file ini yang menunjukkan format penyimpanan key atau adanya rotasi — klaim persistensi hanya berasal dari komentar, implementasinya di dependency eksternal (tor-hsservice 0.43.0) yang tidak diaudit di sini. Doc-comment: 'Setiap peer menjalankan satu onion service persisten (alamat stabil, disimpan di state dir arti)'. Tidak ada kode for... |
| CB-206 | TorClient::create_bootstrapped(config).await dipanggil langsung tanpa dibungkus timeout eksplisit di file ini; komentar modul hanya menyebut proses ini lambat (~30-60 detik). | src/transport/tor.rs:58-60; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Baris 9 (di luar rentang kutipan tapi masih komentar modul yang sama) berbunyi 'bootstrap Tor lambat (~30-60 dtk)', cocok dengan klaim. |
| CB-207 | Tidak ada retry atau timeout eksplisit di sekitar pemanggilan TorClient::create_bootstrapped pada file ini — jika bootstrap Tor menggantung, launch() berpotensi menggantung tanpa batas dari sisi caller. | src/transport/tor.rs:9-60; module=transport_tor; symbol=TorClient::create_bootstrapped; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | NOT_FOUND | MEDIUM | Hanya menilai apa yang terlihat di file ini; tidak diverifikasi apakah arti-client sendiri punya timeout internal. Tidak ditemukan tokio::time::timeout atau logika retry di sekitar baris 58-60 maupun di seluruh fungsi launch(). |
| CB-208 | Onion service dikonfigurasi via OnionServiceConfigBuilder dengan nickname yang di-parse dari string input; kegagalan parse nickname atau build config dikembalikan sebagai Error::Tor. | src/transport/tor.rs:62-69; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | nickname.parse().map_err(...Error::Tor(format!(...)))? diikuti .build().map_err(...Error::Tor(e.to_string()))?. |
| CB-209 | client.launch_onion_service(svc_cfg) sebetulnya mengembalikan Result<Option<(...)>, _> — bukan Option secara langsung. Error dari Result di-map ke Error::Tor via .map_err() di baris 73, baru kemudian lapisan Option-nya di-unwrap via .ok_or_else(\|\| Error::Tor("onion service disabled di config".into()))? di baris 74. Kesimpulan akhirnya (None -> Err dengan pesan tsb) tetap benar, hanya deskripsi tipe return-nya kurang presisi. | src/transport/tor.rs:71-74; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CORRECTED. | IMPLEMENTED | HIGH | Perbedaan minor pada tipe return, tidak mengubah perilaku yang dijelaskan. |
| CB-210 | Alamat .onion diambil via service.onion_address() (Option, error bila belum tersedia) lalu di-render lengkap tanpa redaksi memakai display_unredacted() dari trait safelog::DisplayRedacted, disimpan sebagai String di field onion_address. | src/transport/tor.rs:76-80; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | service.onion_address().ok_or_else(...)?.display_unredacted().to_string(); import 'use safelog::DisplayRedacted as _;' di baris 18 mengonfirmasi trait-nya. |
| CB-211 | Accept loop dijalankan di task tokio::spawn terpisah: rend_stream dibungkus handle_rend_requests() lalu di-pin, setiap StreamRequest yang masuk diproses satu per satu dalam loop while-let. | src/transport/tor.rs:82-96; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | tokio::spawn(async move { let mut streams = Box::pin(handle_rend_requests(rend_stream)); while let Some(req) = streams.next().await { ... } }); |
| CB-212 | Setiap request rendezvous yang diterima di-accept dengan mengirim Tor RELAY cell CONNECTED kosong (Connected::new_empty()) untuk menandai stream berhasil dibuka. | src/transport/tor.rs:87-87; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | match req.accept(Connected::new_empty()).await { ... } — Connected diimpor dari tor_cell::relaycell::msg::Connected (baris 20). |
| CB-213 | Bila req.accept() sukses, DataStream dikirim ke channel mpsc unbounded (in_tx); bila in_tx.send gagal (consumer/receiver sudah drop), loop accept dihentikan via break. Bila req.accept() gagal (Err(_)), error itu diabaikan sepenuhnya (continue) tanpa logging apa pun. | src/transport/tor.rs:87-95; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Ok(ds) => { if in_tx.send(ds).is_err() { break; } }, Err(_) => continue, — tidak ada pemanggilan logging apa pun di cabang Err. |
| CB-214 | Tidak ditemukan pemanggilan logging/tracing apa pun (mis. log::, tracing::, eprintln!) di seluruh file ini; semua error hanya dikonversi ke Error::Tor(String) via .to_string() lalu di-propagate, atau di-drop diam-diam pada accept loop. | src/transport/tor.rs:1-122; module=transport_tor; symbol=Error::Tor; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | NOT_FOUND | HIGH | Dipindai seluruh 122 baris, tidak ada log::/tracing::/println!/eprintln! di mana pun. |
| CB-215 | connect() sebagai sisi initiator memanggil self.client.connect((onion_host.to_string(), port)).await untuk membuka DataStream keluar ke peer onion, error di-map ke Error::Tor. | src/transport/tor.rs:107-112; module=transport_tor; symbol=TorContext::connect; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Kode persis cocok baris demi baris. |
| CB-216 | accept_timeout() mengunci Mutex pada receiver 'incoming', lalu membungkus rx.recv() dengan tokio::time::timeout(timeout, ...); hasilnya di-flatten dengan .ok().flatten() sehingga caller TIDAK BISA membedakan antara 'waktu habis' dan 'channel sudah tertutup' — keduanya sama-sama mengembalikan None. | src/transport/tor.rs:117-120; module=transport_tor; symbol=TorContext::accept_timeout; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | IMPLEMENTED | HIGH | let mut rx = self.incoming.lock().await; tokio::time::timeout(timeout, rx.recv()).await.ok().flatten() — Err(Elapsed)->None via .ok(), dan Ok(None) (channel closed)->Some(None)->None via .flatten(). Kedua kasus sama-sama None, sesuai klaim. |
| CB-217 | Enam dari delapan titik yang disebut memang mengonversi error asli via .to_string() menjadi Error::Tor(String): baris 56 (config build), 60 (create_bootstrapped), 69 (svc_cfg build), 73 (launch_onion_service), dan 111 (connect). Baris 66 memakai format!("nickname tidak valid: {e}") — bukan pemanggilan literal .to_string(), meski hasil akhirnya sama-sama String. Baris 74 dan 78 BUKAN konversi dari error asli sama sekali — keduanya adalah Option::ok_or_else(...) dengan pesan string statis yang di-.into(); pada titik itu tidak ada 'error asli' library yang hilang karena API arti (launch_onion_service dan onion_address()) sudah mengembalikan None secara langsung, bukan Err. Kesimpulan umum bahwa semua error akhirnya bermuara ke satu varian Error::Tor(String) yang sama tetap benar. | src/transport/tor.rs:56-111; module=transport_tor; symbol=Error::Tor; evidence=Verified JSON module=transport_tor; verdict=CORRECTED. | IMPLEMENTED | HIGH | Baris konversi error tersebar di 56, 60, 66, 69, 73, 74, 78, 111. Generalisasi mekanisme '.to_string()' untuk seluruh 8 baris kurang presisi; lihat rincian per baris di corrected_claim. |
| CB-218 | Tidak ditemukan blok #[test] maupun #[cfg(test)] di file ini — tidak ada unit test untuk TorContext::launch, connect, atau accept_timeout dalam file ini. | src/transport/tor.rs:1-122; module=transport_tor; symbol=TorContext::launch; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | NOT_FOUND | HIGH | Dipindai seluruh file, tidak ada #[test] atau #[cfg(test)]. |
| CB-219 | Doc-comment menyatakan TorContext dibuat sekali saat startup bila flag CLI '--tor' aktif, tapi parsing/pengecekan flag tersebut tidak ada di file ini. | src/transport/tor.rs:10-10; module=transport_tor; symbol=tor module; evidence=Verified JSON module=transport_tor; verdict=CONFIRMED. | NEEDS_CONFIRMATION | LOW | Klaim berasal dari komentar saja; wiring flag CLI berada di luar file yang diaudit. Baris 10 persis: 'dibuat sekali saat startup bila flag `--tor` aktif.' Tidak ada kode parsing flag CLI di file ini. |
| CB-220 | Lines 1-4 are the module doc-comment ('//! Alur layar: Splash → Unlock/Create → Init → Main'), which does match the actual enum Screen's variant order. However the evidence_symbol 'enum Screen' is mislabeled for this line range — the enum itself is defined at lines 60-66 (see next claim), not at lines 1-4. | src/tui/mod.rs:1-4; module=tui; symbol=enum Screen; evidence=Verified JSON module=tui; verdict=CORRECTED. | IMPLEMENTED | HIGH | Substance of the claim is true; only the evidence_symbol/line pairing is imprecise (doc-comment cited under an enum-definition label). |
| CB-221 | enum Screen defines the five top-level screens (Splash, Unlock, Create, Init, Main) that gate both rendering (ui.rs render()) and key dispatch (handle_key). | src/tui/mod.rs:60-66; module=tui; symbol=Screen; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-222 | Within Screen::Main, a Mode enum (Browsing, AddContact, InRoom) further gates keybinding dispatch inside handle_main_key. | src/tui/mod.rs:636-726; module=tui; symbol=handle_main_key; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-223 | Keyboard input is read on a dedicated OS thread (blocking crossterm::event::read(), filtered to KeyEventKind::Press) and forwarded to the async event loop via an unbounded mpsc channel, rather than being polled inside tokio::select! directly. | src/tui/mod.rs:439-451; module=tui; symbol=spawn_input_thread; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-224 | The main event loop is a single tokio::select! over four sources: the keyboard channel, the session-event channel, the Tor-bootstrap channel, and a 100ms tick interval. | src/tui/mod.rs:334-411; module=tui; symbol=tokio::select; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-225 | Starting a session (Enter on a selected contact) aborts any previous conn_task, opens fresh SessionCmd/SessionEvent channels, and spawns a tokio task that calls transport::establish(...) then session::run_session(conn, role, local_sk, Some(peer_pk), o_rx, e_tx). | src/tui/mod.rs:868-926; module=tui; symbol=start_connection; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-226 | RoomState enum (None, Connecting, Handshaking, Open, PeerLeft, Closed, at lines 76-84) is transitioned in handle_session_event (1013-1059) but NOT exclusively there: start_connection directly sets app.room = RoomState::Connecting at line 893, and leave_room directly sets app.room = RoomState::None at line 1006, both outside handle_session_event. | src/tui/mod.rs:1013-1059; module=tui; symbol=handle_session_event; evidence=Verified JSON module=tui; verdict=CORRECTED. | PARTIAL | HIGH | The 'exclusively' qualifier is factually wrong; two other functions mutate app.room directly. |
| CB-227 | leave_room aborts the background connection task, drops the SessionCmd/SessionEvent channels, and clears the in-memory message list, explicitly noting to the user 'Riwayat dibuang' (history discarded). | src/tui/mod.rs:995-1011; module=tui; symbol=leave_room; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-228 | Chat messages exist only in App.messages: Vec<ChatLine> for the lifetime of a room; there is no on-disk persistence of chat history in this file — messages.clear() runs both when a new room starts and when a room is left. | src/tui/mod.rs:176-176; module=tui; symbol=messages field / clear() at 895, 1009; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH | messages.clear() also called at line 895 (start_connection) and 1009 (leave_room). messages.clear() confirmed at line 895 (start_connection) and line 1009 (leave_room). |
| CB-229 | try_unlock reads vault bytes from disk then calls vault::unseal with the raw passphrase bytes; on failure it distinguishes an unreadable vault file ('Vault tidak terbaca.') from a failed unseal ('Passphrase salah atau vault rusak.') but the unseal-failure message does not itself distinguish wrong-passphrase from corrupted-ciphertext. | src/tui/mod.rs:559-581; module=tui; symbol=try_unlock; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Underlying vault::unseal algorithm was not read in this task (identity/vault.rs out of scope), so whether this message discipline is deliberate anti-oracle design cannot be confirmed here. |
| CB-230 | create_vault generates a brand-new KeyBundle::generate(), seals it via vault::seal with the entered passphrase, and writes the sealed bytes to app.vault_path; the UI requires the passphrase to be entered twice and compares them with a plain String != check before proceeding. | src/tui/mod.rs:583-592; module=tui; symbol=create_vault; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Confirmation compare is at line 522 (app.pass_confirm != app.pass_input) inside handle_create_key (497-546); this is a local same-process UX check, not a remote-auth comparison. Confirmation compare verified at line 522 inside handle_create_key (497-546). |
| CB-231 | Passphrase input is accumulated in plain String fields (pass_input, pass_confirm), pushed one char at a time, and reset via .clear() after use in several branches; String::clear() truncates length without guaranteeing the freed heap bytes are zeroed, and no Zeroize/ZeroizeOnDrop wrapper is applied to these fields in this file. | src/tui/mod.rs:165-166; module=tui; symbol=pass_input / pass_confirm; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | MEDIUM | .clear() call sites: 482, 487, 524-525, 530-531. This is a factual API-behavior observation, not a claim of exploitability. .clear() call sites verified at 482, 487, 524, 525, 530, 531. |
| CB-232 | SelfKeys.noise_sk: [u8; 32] (the Noise/X25519 static secret) is stored as a plain byte array on App.keys with no visible Zeroize/ZeroizeOnDrop in this file, and the same static secret is read fresh from app.keys and reused for every new connection attempt. | src/tui/mod.rs:32-38; module=tui; symbol=SelfKeys; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | MEDIUM | Reuse point: start_connection line 908 (let local_sk = keys.noise_sk;). Whether the underlying vault/keypair modules apply zeroization elsewhere cannot be confirmed from this file. Reuse point confirmed at line 908 (let local_sk = keys.noise_sk;) inside start_connection. |
| CB-233 | Contact fingerprint is computed by contacts::fingerprint(ed25519_pub, noise_pub), binding both the Ed25519 identity key and the X25519 Noise key into one displayed string; a code comment states this fingerprint 'HARUS' bind both keys because it is what the user manually verifies out-of-band. | src/tui/mod.rs:286-286; module=tui; symbol=contacts::fingerprint; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Same call pattern also at mod.rs:907 and ui.rs:495-497 (comment) /497 (call). Fingerprint hash algorithm itself is not visible in these two files. Also confirmed at mod.rs:907 and ui.rs:495-497 (comment)/497 (call). |
| CB-234 | The contacts file path is derived deterministically from the vault file's stem plus a '-contacts' suffix and stored in the same directory as the vault. | src/tui/mod.rs:594-600; module=tui; symbol=contacts_file_path; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-235 | A 32-byte contacts_key is derived once at unlock/create time via contacts::derive_contacts_key(&bundle) and passed to contacts::load_contacts/save_contacts as the key argument for the on-disk contacts store. | src/tui/mod.rs:182-182; module=tui; symbol=contacts_key field; evidence=Verified JSON module=tui; verdict=CONFIRMED. | PARTIAL | MEDIUM | Derivation call sites: 569, 587. Whether/how the store is encrypted with this key is implemented in the contacts module, not visible in tui/mod.rs. Derivation call sites verified at 569 (try_unlock) and 587 (create_vault). |
| CB-236 | On unlock, contacts loaded from disk are merged with any contacts passed in at startup, de-duplicated by ed25519_pub equality (startup contacts inserted at front only if not already present), then immediately re-persisted to disk. | src/tui/mod.rs:602-615; module=tui; symbol=load_contacts_into; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-237 | persist_contacts failure only raises a non-blocking warning notification ('Peringatan: gagal menyimpan kontak ke disk.'); there is no retry and the in-memory contact list is not rolled back, so UI state and disk state can silently diverge. | src/tui/mod.rs:617-623; module=tui; symbol=persist_contacts; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-238 | load_contacts_into silently ignores a failed contacts::load_contacts call via an `if let Ok(disk) = ...` pattern, falling back to only the startup-supplied contacts with no user-facing error message. | src/tui/mod.rs:602-606; module=tui; symbol=contacts::load_contacts; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-239 | Global Ctrl+C is checked first in handle_key (before any screen-specific dispatch) and immediately terminates the TUI loop from any screen without explicitly aborting an in-flight app.conn_task or notifying the peer session; the same unconditional-quit pattern applies to Esc on Splash/Unlock/Create/Init and to q/Esc while browsing contacts. | src/tui/mod.rs:459-461; module=tui; symbol=mod module; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | MEDIUM | Additional Esc-quit sites: 479 (Unlock), 499 (Create), 550 (Init), 655 (Browsing q/Esc). Actual task cleanup on process exit depends on tokio runtime shutdown behavior not visible in this file. Esc-quit sites verified at 479 (Unlock), 499 (Create), 550 (Init), 655 (Browsing q/Esc); none of these paths abort conn_task. |
| CB-240 | handle_session_event treats SessionEvent::Notice as non-fatal (only shows a warning + system chat line, room state untouched, per an explicit code comment) while SessionEvent::Error forces RoomState::Closed and shows an error notification plus a system chat line. | src/tui/mod.rs:1048-1057; module=tui; symbol=SessionEvent::Notice; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-241 | There is no file or structured logging in this module; user-visible status is only an ephemeral in-memory Notification (levels Error/Warn/Success/Info). Success notices auto-dismiss after 30 ticks (~3s at 100ms/tick) and Info after 40 ticks (~4s), while Error/Warn persist until replaced. | src/tui/mod.rs:121-141; module=tui; symbol=Notification; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-242 | The terminal must be at least 80x24 characters (MIN_WIDTH/MIN_HEIGHT); below that, render() replaces the entire screen with a size-warning card via render_too_small instead of any interactive UI. | src/tui/ui.rs:41-49; module=tui; symbol=ui module; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-243 | Chat scrolling and 'Mode Light' recency dimming (last BLUR_KEEP_RECENT=3 messages stay unblurred) both depend on estimate_wrapped_lines, which the code's own 'ponytail' comment documents as a char-count/effective-width approximation of ratatui's real word-boundary wrap, not an exact replication — long-word-heavy text can make the scroll/dim estimate diverge from actual rendered line counts. | src/tui/ui.rs:756-767; module=tui; symbol=estimate_wrapped_lines; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH | BLUR_KEEP_RECENT defined at mod.rs:212; used at ui.rs:583 and 609. Minor imprecision in the claim's own note: BLUR_KEEP_RECENT is used literally only at ui.rs:583; line 609 uses the derived variable keep_clear_from, not the constant token directly. Does not affect the core claim. |
| CB-244 | 'Mode Light' can be scoped locally ([L], nothing sent to peer) or synced ([B], sends SessionCmd::Blur(true) to the peer session) via a modal prompt; toggling off only re-sends SessionCmd::Blur(false) if the mode had previously been synced. | src/tui/mod.rs:730-774; module=tui; symbol=handle_blur_prompt_key / toggle_blur; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-245 | Reply quoting clips the quoted text to QUOTE_MAX=40 characters with a trailing ellipsis before prefixing it to the outgoing message text via format_reply; the composite text is what actually gets sent as SessionCmd::Text. | src/tui/mod.rs:861-866; module=tui; symbol=format_reply; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH | Send path: send_message, lines 976-993, SessionCmd::Text at line 988. Send path confirmed at send_message, lines 976-993, SessionCmd::Text at line 988. |
| CB-246 | Message search (Ctrl+S) is a case-insensitive substring match over each message's text using to_lowercase().contains(...), recomputed on every keystroke; an empty query yields zero matches by design. | src/tui/mod.rs:813-823; module=tui; symbol=search_messages; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-247 | The invite-code popup splits the invite string into fixed 44-byte chunks of its raw bytes for display (invite.as_bytes().chunks(44)) and decodes each chunk with str::from_utf8(..).unwrap_or("") — a chunk boundary that split a multi-byte UTF-8 codepoint would silently render as empty text for that chunk; this only affects the on-screen popup, since copy_invite's clipboard path copies the original, unchunked String. | src/tui/ui.rs:899-905; module=tui; symbol=str::from_utf8; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | MEDIUM | copy_invite (clipboard) is at src/tui/mod.rs:625-634. copy_invite (clipboard) confirmed at src/tui/mod.rs:625-634, copies invite.clone() unchunked. |
| CB-248 | ChatLine.time is stamped with chrono::Local::now() at the moment a message is pushed into the local `messages` Vec (for both own and peer messages); the struct's own doc comment explicitly notes this is local UI time, not a timestamp carried over the wire/protocol. | src/tui/mod.rs:92-108; module=tui; symbol=ChatLine; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-249 | Test blur_prompt_local_activates_without_sending verifies that pressing 'l' in the Mode Light prompt enables blur locally, leaves blur_synced false, closes the prompt, and sends nothing on the SessionCmd channel. | src/tui/mod.rs:1074-1084; module=tui; symbol=blur_prompt_local_activates_without_sending; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-250 | Test blur_prompt_both_sends_command verifies that pressing 'b' in the Mode Light prompt enables blur, sets blur_synced true, and sends SessionCmd::Blur(true) on the channel. | src/tui/mod.rs:1087-1096; module=tui; symbol=SessionCmd::Blur; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-251 | Test blur_prompt_esc_cancels verifies that pressing Esc while the Mode Light prompt is open closes the prompt without enabling blur. | src/tui/mod.rs:1099-1106; module=tui; symbol=blur_prompt_esc_cancels; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-252 | Test toggle_blur_off_sends_command_when_synced verifies that toggling off an active+synced Mode Light disables both flags and sends SessionCmd::Blur(false). | src/tui/mod.rs:1109-1119; module=tui; symbol=SessionCmd::Blur; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-253 | Test format_reply_clips_long_quote verifies a 50-character quote is truncated with an ellipsis while the reply body still appears intact after it. | src/tui/mod.rs:1121-1127; module=tui; symbol=format_reply_clips_long_quote; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-254 | Test format_reply_keeps_short_quote_intact verifies a short quote ('halo') is not truncated or ellipsized. | src/tui/mod.rs:1129-1134; module=tui; symbol=format_reply_keeps_short_quote_intact; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-255 | Test search_messages_case_insensitive verifies substring search matches regardless of case ('dunia' matches 'Halo Dunia') and that empty or no-match queries return an empty result vector. | src/tui/mod.rs:1136-1146; module=tui; symbol=search_messages_case_insensitive; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-256 | Test estimate_wrapped_lines_short_text_is_one_line verifies that short text ('halo') at width 80 estimates to exactly 1 line. | src/tui/ui.rs:998-1001; module=tui; symbol=estimate_wrapped_lines_short_text_is_one_line; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-257 | Test estimate_wrapped_lines_long_text_spans_multiple_lines verifies that a 200-character string at width 80 (effective width 76 after the 4-char prefix allowance) estimates to 3 lines, documenting the ceil(len/effective_width) formula. | src/tui/ui.rs:1003-1008; module=tui; symbol=estimate_wrapped_lines_long_text_spans_multiple_lines; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-258 | Test estimate_wrapped_lines_zero_width_does_not_panic verifies the estimator does not panic/divide-by-zero when width is 0 (falls back to an effective width of 1 via .max(1)), returning 4 for a 4-character string. | src/tui/ui.rs:1010-1013; module=tui; symbol=estimate_wrapped_lines_zero_width_does_not_panic; evidence=Verified JSON module=tui; verdict=CONFIRMED. | IMPLEMENTED | HIGH |  |
| CB-259 | The #[cfg(test)] modules in both files cover only pure logic helpers (Mode Light toggle/prompt, reply formatting, message search, wrap-line estimation); no test in these two files exercises vault unlock/create, contact add/delete/persist-to-disk, or the network session-establishment path (start_connection, handle_session_event) — those are only reachable through manual or integration-level exercising outside these files. | src/tui/mod.rs:1061-1147; module=tui; symbol=mod module; evidence=Verified JSON module=tui; verdict=CONFIRMED. | NEEDS_CONFIRMATION | MEDIUM | Based on absence within the two test modules read (mod.rs:1061-1147, ui.rs:994-1014); other test files elsewhere in the crate were not reviewed in this task. Full test modules read in both files (mod.rs:1061-1147 and ui.rs:994-1014); confirmed no vault/contacts/session-establishment tests present in either. |
