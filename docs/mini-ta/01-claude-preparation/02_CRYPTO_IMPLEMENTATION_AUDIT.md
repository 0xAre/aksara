# 02 - Audit Implementasi Kriptografi AKSARA

Audit ini hanya menyintesis field `primitives` dari 8 raw JSON lokal dan versi crate yang sudah tercatat di `PROGRESS.md`/`Cargo.toml`. Tidak ada referensi internet dan tidak ada audit source dependency eksternal.

## Inventaris Primitif

| ID | Primitif | Algoritma | Fungsi | Parameter | Library | Evidence | Status | Confidence | Risiko |
|----|-----------|-----------|--------|-----------|---------|----------|--------|------------|--------|
| CR-001 | ChaCha20Poly1305 contact-store encryption | ChaCha20-Poly1305 (RFC 8439) | Mengenkripsi (save_contacts) dan mendekripsi (load_contacts) seluruh isi contact store sebelum ditulis/setelah dibaca dari disk | key_size_bits: 256 bit (32 byte) — dari Key::from_slice(key) dengan key: &[u8;32] (baris 172, 193); nonce_or_iv_size_bytes: 12 byte (96 bit) — const C_NONCE_LEN = 12 (baris 100); tag_size_bytes: 16 byte (128 bit) — terlihat dari pengecekan panjang minimum file `C_NONCE_LEN + 16` di baris 189 (buk... | chacha20poly1305; version=0.10.1 (dependensi chacha20 0.10.0, poly1305 0.8.0, aead 0.5.2) | src/contacts/mod.rs:170-198; module=contacts; symbol=ChaCha20Poly1305 contact-store encryption; verdict=CONFIRMED | IMPLEMENTED | HIGH | Nonce dibangkitkan acak baru setiap save_contacts via OsRng (baris 173), bukan nonce statis/counter, sehingga risiko nonce-reuse pada kunci yang sama relatif rendah untuk skala pemakaian lokal (96-bit random). Tidak ada AAD dipakai (baris 174-176 hanya plaintext bytes) sehingga ciphertext tidak terikat ke konteks eksternal (mis. path/versi format). Kunci ... |
| CR-002 | BLAKE2s256 identity fingerprint | BLAKE2s-256 | fingerprint(ed25519_pub, noise_pub) menghasilkan identifier publik untuk pencocokan invite/discovery (baris 48-54) | key_size_bits: N/A (fungsi hash tanpa kunci rahasia); nonce_or_iv_size_bytes: N/A; tag_size_bytes: digest output 32 byte / 256 bit | blake2; version=0.10.6 | src/contacts/mod.rs:48-54; module=contacts; symbol=BLAKE2s256 identity fingerprint; verdict=CONFIRMED | IMPLEMENTED | HIGH | Domain-separation string 'aksara-fingerprint-v1' mencegah tabrakan lintas-penggunaan BLAKE2s lain di codebase (mis. dengan kunci derivation di primitive KDF di bawah). Kedua public key WAJIB diserap bersama (komentar baris 42-47) untuk mencegah serangan invite susun-ulang. Karena ini hash atas data publik tanpa kunci rahasia (bukan MAC), fingerprint TIDAK... |
| CR-003 | BLAKE2s256-based contacts key derivation | BLAKE2s-256 dipakai sebagai fungsi derivasi kunci single-shot (bukan konstruksi KDF khusus seperti HKDF) | derive_contacts_key(bundle) menurunkan kunci ChaCha20Poly1305 32-byte untuk enkripsi contact store dari secret identity yang sudah ter-unlock (baris 104-112) | key_size_bits: 256 bit (32 byte); nonce_or_iv_size_bytes: N/A; tag_size_bytes: N/A | blake2; version=0.10.6 | src/contacts/mod.rs:104-112; module=contacts; symbol=BLAKE2s256-based contacts key derivation; verdict=CONFIRMED | IMPLEMENTED | MEDIUM | Ini hash biasa (BLAKE2s256) dipakai sebagai KDF, bukan konstruksi KDF khusus (mis. HKDF); dapat diterima karena input sudah berentropi tinggi (secret identity) dan hanya satu context string ('aksara-contacts-key-v1') dipakai untuk satu tujuan tunggal di file ini, jadi tidak ada bukti reuse/collision antar-turunan-kunci yang terlihat. Tidak terlihat pembun... |
| CR-004 | OsRng CSPRNG untuk nonce ChaCha20Poly1305 | OsRng (rand crate, entropy dari OS) | Membangkitkan nonce 12-byte acak untuk tiap pemanggilan save_contacts via ChaCha20Poly1305::generate_nonce(&mut OsRng) (import baris 24, dipakai baris 173) | nonce_or_iv_size_bytes: 12 byte / 96 bit | rand; version=0.8.6 | src/contacts/mod.rs:173-176; module=contacts; symbol=OsRng CSPRNG untuk nonce ChaCha20Poly1305; verdict=CONFIRMED | IMPLEMENTED | HIGH | Nonce baru dibangkitkan acak setiap save (bukan nonce statis/counter), mengurangi risiko nonce reuse pada kunci yang sama antar pemanggilan save_contacts berulang. Tetap probabilistik (bukan dijamin unik secara deterministik) sehingga secara teori ada risiko collision pada ruang nonce 96-bit jika volume penyimpanan sangat besar dengan kunci sama — namun u... |
| CR-005 | Base64 URL-safe (no padding) invite code encoding | Base64 URL_SAFE_NO_PAD (RFC 4648 §5) | encode_invite/decode_invite mengubah pasangan public key (64 byte raw) menjadi/dari string invite yang dibagikan out-of-band, dengan onion address opsional di-suffix '@host' (baris 59-92) |  | base64; version=0.22.1 | src/contacts/mod.rs:59-92; module=contacts; symbol=Base64 URL-safe (no padding) invite code encoding; verdict=CONFIRMED | IMPLEMENTED | HIGH | Invite code TIDAK ditandatangani/diautentikasi (dicatat eksplisit di komentar baris 42-47) — base64 di sini murni serialisasi transport, bukan proteksi integritas/otentikasi. Tidak ada magic prefix yang disengaja, dikonfirmasi test invite_has_no_obvious_prefix (baris 316-323), untuk menghindari fingerprinting metadata protokol oleh pengamat pasif. |
| CR-006 | Hex encoding untuk representasi key (fingerprint output & format plaintext kontak) | Hex (base16) encode/decode | hex::encode dipakai untuk output fingerprint (baris 53) dan untuk menyerialisasi ed25519_pub/noise_pub tiap kontak ke format teks sebelum dienkripsi (baris 121-122); hex::decode dipakai balik di parse_key32 (baris 131... |  | hex; version=0.4.3 | src/contacts/mod.rs:114-138; module=contacts; symbol=Hex encoding untuk representasi key (fingerprint output & format plaintext kontak); verdict=CORRECTED | IMPLEMENTED | HIGH | Hex encoding deterministik dan reversibel penuh, tidak ada risiko kriptografis langsung dari encoding itu sendiri; risiko utamanya ada di level penanganan error (silent-skip pada data korup/invalid) yang tercatat terpisah sebagai klaim error_handling. Semua sitasi baris di dalam deskripsi akurat: hex::encode untuk fingerprint di baris 53, hex::encode ed/n... |
| CR-007 | X25519 Diffie-Hellman (Noise DH token es/ss/ee/se) via snow 0.10.0 | X25519 (Curve25519 ECDH) | Key agreement steps (es, ss, ee, se) dalam Noise_IK handshake, dikonfigurasi lewat nama pattern '25519' dan dijalankan otomatis oleh snow::Builder/HandshakeState saat write_message/read_message dipanggil. | key_size_bits: 256 (array [u8;32] pada parameter fungsi, baris 32 dan 43) | snow; version=0.10.0 | src/crypto/handshake.rs:21-47; module=crypto_handshake; symbol=X25519 Diffie-Hellman (Noise DH token es/ss/ee/se) via snow 0.10.0; verdict=CONFIRMED | IMPLEMENTED | MEDIUM | Pembangkitan ephemeral key ('e') dan komputasi DH sepenuhnya internal ke crate snow — tidak dapat diverifikasi/diaudit dari file ini. Tidak ada pengecekan eksplisit di handshake.rs terhadap kunci publik peer yang all-zero atau titik lemah (low-order point) — kepercayaan sepenuhnya pada implementasi snow. |
| CR-008 | ChaCha20-Poly1305 (Noise 'ChaChaPoly') untuk handshake payload & transport messages | ChaCha20-Poly1305 | Enkripsi/dekripsi payload handshake (write_message/read_message, baris 52-60) DAN enkripsi/dekripsi pesan transport pasca-handshake (EncryptedSession::encrypt/decrypt, baris 91-98). | nonce_or_iv_size_bytes: Tidak terlihat di file ini — nonce/counter Noise dikelola sepenuhnya internal oleh snow::HandshakeState/TransportState, tidak ada parameter nonce yang di-pass dari handshake.rs.; tag_size_bytes: 16 byte (128 bit) — dinyatakan eksplisit di komentar baris 90: 'out harus puny... | snow; version=0.10.0 | src/crypto/handshake.rs:21-98; module=crypto_handshake; symbol=ChaCha20-Poly1305 (Noise 'ChaChaPoly') untuk handshake payload & transport messages; verdict=UNVERIFIED_ENTRY | IMPLEMENTED | MEDIUM | Manajemen nonce sepenuhnya didelegasikan ke snow dan tidak dapat diverifikasi dari file ini. API EncryptedSession hanya mengekspos encrypt/decrypt lewat &mut self secara sekuensial sehingga wrapper ini sendiri tidak menyediakan cara untuk memanggil ulang dengan nonce yang sama; namun tidak ada proteksi eksplisit di file ini terhadap reordering/replay pesa... |
| CR-009 | BLAKE2s sebagai fungsi hash Noise (transcript hash / HKDF) | BLAKE2s | Dipakai internal oleh snow untuk mix_hash (transcript hash 'h') dan sebagai basis HKDF (mix_key) selama Noise_IK handshake — tidak ada pemanggilan langsung fungsi hash di file ini, hanya dikonfigurasi lewat token 'BLA... |  | snow; version=0.10.0 | src/crypto/handshake.rs:21-21; module=crypto_handshake; symbol=BLAKE2s sebagai fungsi hash Noise (transcript hash / HKDF); verdict=UNVERIFIED_ENTRY | NEEDS_CONFIRMATION | LOW | Tidak ada pemanggilan eksplisit di file ini untuk diaudit; domain separation dan konstruksi HKDF sepenuhnya bergantung pada implementasi internal snow, tidak dapat diverifikasi dari handshake.rs/mod.rs. |
| CR-010 | Noise HKDF (key derivation dari DH output, berbasis BLAKE2s) via snow 0.10.0 | HKDF (Noise Protocol construction, berbasis BLAKE2s) | Derivasi kunci sesi ChaChaPoly dari hasil DH (es/ss/ee/se) selama handshake — hanya tersirat dari nama pattern, tidak ada kode derivasi kunci eksplisit terlihat di handshake.rs. |  | snow; version=0.10.0 | src/crypto/handshake.rs:21-21; module=crypto_handshake; symbol=Noise HKDF (key derivation dari DH output, berbasis BLAKE2s) via snow 0.10.0; verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | Karena seluruh proses KDF berada di dalam crate snow dan tidak ada satu baris kode pun di handshake.rs/mod.rs yang memanggilnya langsung, klaim ini murni inferensi dari nama pattern string — tidak bisa diverifikasi lebih jauh tanpa membaca source snow. Sama seperti BLAKE2s — murni inferensi dari nama pattern, sudah tepat dihedge LOW confidence. |
| CR-011 | Key serialization kunci Noise sebagai raw byte array [u8;32] | Tidak ada format serialisasi khusus — raw fixed-size byte array | local_noise_privkey dan peer_noise_pubkey diterima sebagai &[u8;32] mentah (bukan PEM/DER/base64/hex) lalu diteruskan langsung ke snow::Builder::local_private_key()/remote_public_key(). |  | snow; version=0.10.0 | src/crypto/handshake.rs:32-46; module=crypto_handshake; symbol=Key serialization kunci Noise sebagai raw byte array [u8;32]; verdict=CONFIRMED | IMPLEMENTED | HIGH | Tipe [u8;32] memaksa panjang kunci benar di level compile-time untuk parameter fungsi, mengurangi risiko truncation/panjang salah dibanding representasi string; tidak ada risiko misuse serialisasi tambahan yang terlihat di file ini. |
| CR-012 | CSPRNG untuk pembangkitan ephemeral/static key | Tidak terlihat di file ini | Tidak ada pemanggilan CSPRNG langsung di handshake.rs — pembangkitan ephemeral key ('e') terjadi internal di snow saat write_message/read_message; pembangkitan static keypair (NoiseKey::generate(), dipakai di test bar... |  | Tidak terlihat di handshake.rs/mod.rs (kandidat: rand 0.8.6 dipakai di tempat lain sesuai daftar resolusi Cargo.lock, tapi tidak diimpor/dipanggil di kedua file ini); version=ti... | src/crypto/handshake.rs:18-18; module=crypto_handshake; symbol=CSPRNG untuk pembangkitan ephemeral/static key; verdict=CONFIRMED | NOT_FOUND | HIGH | Tidak dapat dinilai dari file ini karena sumber entropi tidak terlihat/tidak diimpor di handshake.rs maupun mod.rs. Akurat: tidak ada import rand/CSPRNG di handshake.rs (hanya snow dan crate::error::Error, baris 18-19). Referensi NoiseKey::generate() dipakai test di baris 133-134 (dan berulang di 149-150, 176-177, 195-197), sesuai klaim; implementasi gene... |
| CR-013 | ChaCha20-Poly1305 AEAD (vault encryption) | ChaCha20-Poly1305 (IETF construction, 96-bit nonce) | Encrypts/decrypts the 64-byte secret material (32B Ed25519 signing key + 32B X25519 secret) for at-rest storage in the identity vault file. Used by seal() (vault.rs:60-93) and unseal() (vault.rs:98-128). | key_size_bits: 256 bits (32 bytes) — Key::from_slice(&*enc_key) where enc_key: Zeroizing<[u8;32]> from derive_key() (vault.rs:49-56, key buffer declared [0u8;32] at line 51); nonce_or_iv_size_bytes: 12 bytes / 96 bits — const NONCE_LEN: usize = 12 (vault.rs:29); nonce generated via ChaCha20Poly13... | chacha20poly1305 0.10.1 (deps: chacha20 0.10.0, poly1305 0.8.0, aead 0.5.2) | src/identity/vault.rs:60-128; module=identity; symbol=ChaCha20-Poly1305 AEAD (vault encryption); verdict=CONFIRMED | IMPLEMENTED | HIGH | (1) No AAD/domain separation is used, so nothing cryptographically binds a given ciphertext to a specific vault/context beyond the vault's own byte layout. (2) Nonce is randomly generated (not counter-based) each call; catastrophic under key reuse, but no code path in this file reuses a single derived key across multiple encrypt calls — each seal() re-der... |
| CR-014 | Argon2id password-based key derivation | Argon2id, version 0x13 (19) | Derives the 256-bit ChaCha20Poly1305 vault-encryption key from the user's passphrase and a random salt, used identically in both seal() and unseal() via derive_key(). | key_size_bits: 256 bits (32 bytes) output — Params::new(19*1024, 2, 1, Some(32)) (vault.rs:40-45) | argon2 0.5.3 | src/identity/vault.rs:39-56; module=identity; symbol=Argon2id password-based key derivation; verdict=CONFIRMED | IMPLEMENTED | HIGH | Output is used directly as the AEAD key with no intermediate HKDF/domain-separation step. The code comment (vault.rs:33-38) claims the parameters follow 'OWASP 2024' guidance and take '~100ms' on modern hardware; the parameter values (19 MiB / t=2 / p=1) are directly verified in code, but the timing claim is unverified by any benchmark/test in these files. |
| CR-015 | Ed25519 identity keypair (signing/verifying key) | Ed25519 | Long-term identity key used to derive a public identity (doc comment: shared with contacts via invite code, keypair.rs:6-7); consumed by vault.rs for at-rest serialization. | key_size_bits: Secret/signing key: 256 bits (32 bytes) — explicit [u8;32] in from_secret_bytes(bytes: [u8;32]) (keypair.rs:24) and secret_bytes() -> &[u8;32] (keypair.rs:31-33). Public/verifying key: standard Ed25519 is 256 bits/32 bytes, but this file does not declare an explicit 32-byte array/c... | ed25519-dalek 2.2.0 | src/identity/keypair.rs:6-38; module=identity; symbol=Ed25519 identity keypair (signing/verifying key); verdict=CONFIRMED | PARTIAL | HIGH | This file contains ONLY key generation/storage for the Ed25519 identity key — no sign()/verify() call sites exist in keypair.rs, vault.rs, or mod.rs, so signature-side misuse (message/domain-separation practices, signature malleability handling, etc.) cannot be assessed from these files. Zeroization of the secret is explicit; the public verifying key is c... |
| CR-016 | X25519 Noise keypair (Diffie-Hellman key agreement) | X25519 (Curve25519 ECDH) | Documented (comment, keypair.rs:40-41) as the DH key used for the Noise_IK handshake; only key generation/storage is present in this file, not the handshake itself. | key_size_bits: Secret: 256 bits (32 bytes) — X25519Secret::from(bytes: [u8;32]) (keypair.rs:58-59), secret_bytes() -> [u8;32] (keypair.rs:65-67). Public: 256 bits (32 bytes) — public_bytes() -> [u8;32] (keypair.rs:69-71), explicit in-file. | x25519-dalek 2.0.1 | src/identity/keypair.rs:40-72; module=identity; symbol=X25519 Noise keypair (Diffie-Hellman key agreement); verdict=CONFIRMED | PARTIAL | HIGH | Only key generation/storage is in scope of these files; the actual Noise_IK handshake (snow 0.10.0 per project dependencies) where this key would be consumed is not present here, so DH-output handling and any handshake-transcript/nonce practices cannot be assessed from these three files alone. Zeroization is explicit via ZeroizeOnDrop. |
| CR-017 | OsRng (CSPRNG for key/salt/nonce generation) | OS-provided CSPRNG (rand::rngs::OsRng) | Randomness source for: Ed25519 key generation (keypair.rs:19), X25519 key generation (keypair.rs:53), Argon2id salt generation (vault.rs:68-69), ChaCha20Poly1305 nonce generation (vault.rs:75). |  | rand 0.8.6 | src/identity/keypair.rs:2-2; module=identity; symbol=OsRng (CSPRNG for key/salt/nonce generation); verdict=CONFIRMED | IMPLEMENTED | HIGH | Standard OS CSPRNG usage for all key/salt/nonce generation observed in these files; no custom RNG or manual seeding logic present. Correctness ultimately relies on the underlying OS RNG implementation, which is outside the scope of this source-level review. Also used in src/identity/vault.rs at lines 20 (import), 68-69 (salt fill_bytes), and 75 (nonce gen... |
| CR-018 | ChaCha20Poly1305 96-bit random nonce | 96-bit (12-byte) random nonce via ChaCha20Poly1305::generate_nonce |  | nonce_or_iv_size_bytes: 12 bytes / 96 bits — const NONCE_LEN: usize = 12 (vault.rs:29) | chacha20poly1305 0.10.1 (aead 0.5.2 AeadCore trait) | src/identity/vault.rs:74-75; module=identity; symbol=ChaCha20Poly1305 96-bit random nonce; verdict=CONFIRMED | IMPLEMENTED | HIGH | Random (not counter-based) 96-bit nonce generation. Since the AEAD key is also re-derived per seal() call from a fresh random Argon2id salt, there is no code path in this file where the same key is reused across multiple encryptions — the scenario where random-nonce birthday-bound collision risk would matter most for a single key. No misuse-resistant (e.g... |
| CR-019 | Vault binary layout / raw secret-key serialization | Fixed-offset byte concatenation, no ASN.1/PEM/other standard encoding |  |  | n/a (hand-written, std only) | src/identity/vault.rs:1-31; module=identity; symbol=Vault binary layout / raw secret-key serialization; verdict=CONFIRMED | IMPLEMENTED | HIGH | No format versioning means any future change to PLAINTEXT_LEN or field order would be a breaking, silent change with no migration path detectable in this file. No integrity/authenticity check exists on the salt or nonce themselves in isolation (only the ciphertext is authenticated via the Poly1305 tag as part of the AEAD construction) — salt/nonce are con... |
| CR-020 | rustls default CryptoProvider (ring) | Instalasi CryptoProvider default rustls berbasis ring; algoritma TLS spesifik (cipher suite dsb.) tidak dirinci di titik pemanggilan ini. | Dipanggil sekali di real_main() sebelum bootstrap Tor (baik jalur print_id maupun jalur normal), supaya arti-client (Tor client) punya TLS crypto provider default saat online. |  | rustls 0.23.40 (backend ring 0.17.14); version=rustls 0.23.40 / ring 0.17.14 | src/main.rs:213-215; module=main_and_error; symbol=rustls default CryptoProvider (ring); verdict=CONFIRMED | IMPLEMENTED | HIGH | Karena Result diabaikan sepenuhnya, tidak ada cara dari kode ini untuk membedakan 'sudah terpasang (aman diabaikan)' vs kegagalan lain yang mestinya fatal — berpotensi program lanjut tanpa CryptoProvider valid pada skenario yang tidak dites di sini. |
| CR-021 | Ed25519 identity public key access (to_bytes) | Ed25519, via ed25519-dalek PublicKey::to_bytes() (nama tipe persis KeyBundle.identity tidak terlihat langsung di main.rs, disimpulkan dari nama field & import identity::keypair::KeyBundle). | build_self_keys() mengambil byte public key identitas untuk dimasukkan ke struct SelfKeys (dipakai print invite/fingerprint dan diteruskan ke tui::run()). | key_size_bits: Tidak dinyatakan eksplisit di main.rs/error.rs — panjang array hasil to_bytes() tidak terlihat di titik pemanggilan ini (implementasi ada di src/identity/keypair.rs, di luar cakupan tugas). | ed25519-dalek 2.2.0; version=2.2.0 | src/main.rs:187-188; module=main_and_error; symbol=Ed25519 identity public key access (to_bytes); verdict=CONFIRMED | PARTIAL | MEDIUM | Ini public key sehingga eksposur bukan masalah kerahasiaan; risiko utama adalah tidak ada verifikasi di sini bahwa byte ini konsisten dipakai sebagai identitas jangka panjang tanpa domain separation eksplisit — namun domain separation (jika ada) tidak terlihat dari main.rs. |
| CR-022 | X25519 (Noise) key material access (public_bytes/secret_bytes) | X25519 (via x25519-dalek), dipakai sebagai static key untuk handshake Noise (deskripsi Noise_IK ada di project CLAUDE.md, tidak diverifikasi ulang langsung dari isi crypto module yang tidak dibaca untuk tugas ini). | build_self_keys() mengambil byte public dan SECRET key noise dari KeyBundle untuk dimasukkan ke struct SelfKeys, yang lalu diteruskan ke tui::run(). | key_size_bits: Tidak dinyatakan eksplisit di main.rs/error.rs. | x25519-dalek 2.0.1; version=2.0.1 | src/main.rs:189-190; module=main_and_error; symbol=X25519 (Noise) key material access (public_bytes/secret_bytes); verdict=CONFIRMED | PARTIAL | MEDIUM | secret_bytes() mengekstrak kunci PRIVAT X25519 mentah ke variabel lokal noise_sk (baris 190) lalu disalin ke struct SelfKeys (baris 193-199) yang diteruskan ke tui::run(). Tidak terlihat di main.rs apakah tipe KeyBundle/SelfKeys mengimplementasikan ZeroizeOnDrop untuk secret ini — definisi struct-nya berada di modul identity/tui yang tidak dibaca untuk tu... |
| CR-023 | KeyBundle::generate() — entrypoint pembangkitan pasangan kunci baru | Tidak diketahui persis dari main.rs/error.rs. Nama field KeyBundle (identity, noise) mengindikasikan gabungan pembangkitan Ed25519 + X25519, tapi implementasi (termasuk sumber randomness) ada di src/identity/keypair.r... | Dipanggil hanya saat file vault belum ada di disk, dalam load_or_create_vault() (main.rs baris 167-185), setelah passphrase non-kosong divalidasi. |  | Kandidat berdasar Cargo.lock: ed25519-dalek 2.2.0, x25519-dalek 2.0.1, rand 0.8.6 — pemakaian persisnya tidak dikonfirmasi dari dua file yang dibaca. | src/main.rs:179-179; module=main_and_error; symbol=KeyBundle::generate() — entrypoint pembangkitan pasangan kunci baru; verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | Sumber randomness (CSPRNG) yang dipakai tidak dapat dipastikan dari main.rs/error.rs saja — perlu pembacaan src/identity/keypair.rs untuk verifikasi (mis. apakah pakai OsRng dari rand 0.8.6). |
| CR-024 | Vault seal/unseal (proteksi identitas berbasis passphrase) | TIDAK diverifikasi langsung dari main.rs/error.rs. Deskripsi tingkat-repo (CLAUDE.md project) menyebut 'vault identitas Argon2id/ChaCha20-Poly1305', namun klaim algoritma persis ini TIDAK dikonfirmasi dari isi src/ide... | load_or_create_vault() di main.rs memanggil vault::read_vault()+vault::unseal() untuk membuka vault yang sudah ada, atau vault::seal()+vault::write_vault() untuk membuat vault baru. |  | Kandidat dari Cargo.lock: argon2 0.5.3, chacha20poly1305 0.10.1 (chacha20 0.10.0, poly1305 0.8.0, aead 0.5.2) — pemakaian persis di titik ini tidak dikonfirmasi tanpa membaca sr... | src/main.rs:167-185; module=main_and_error; symbol=Vault seal/unseal (proteksi identitas berbasis passphrase); verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | Desain pesan error ambigu (error.rs baris 1-3, 12-14) secara eksplisit dimaksudkan mencegah oracle attack lewat isi pesan error saat proses unseal gagal. Ini mitigasi yang relevan dan terlihat jelas di kode, tapi hanya mencakup channel pesan error tekstual — potensi side-channel lain (mis. timing perbedaan proses Argon2id vs pembacaan file) tidak bisa din... |
| CR-025 | Noise protocol error passthrough (snow::Error) | Noise (kemungkinan Noise_IK sesuai deskripsi project CLAUDE.md) — implementasi handshake ada di modul crypto yang TIDAK dibaca untuk tugas ini; di error.rs hanya tipe error snow::Error yang terlihat, bukan logika hand... | Varian Error::Noise(#[from] snow::Error) menyediakan jalur konversi otomatis error handshake Noise (dari crate snow) ke tipe Error aplikasi, dipakai lewat operator `?` di modul lain (tidak ada pemakaian langsung di ma... |  | snow 0.10.0; version=0.10.0 | src/error.rs:16-17; module=main_and_error; symbol=Noise protocol error passthrough (snow::Error); verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | Karena source() masih membawa snow::Error asli, kode LAIN yang secara eksplisit memanggil source()/Debug pada Error bisa saja membocorkan detail Noise meskipun Display tidak — hal ini tidak bisa diverifikasi ada/tidaknya dari main.rs/error.rs saja. |
| CR-026 | Enkripsi/dekripsi pesan sesi (session.encrypt / session.decrypt di atas EncryptedSession hasil handshake) | Algoritma AEAD konkret TIDAK disebutkan literal di file session/mod.rs — dipanggil lewat method session.encrypt()/session.decrypt() pada nilai bertipe hasil HandshakeSession::into_transport() (didefinisikan di crate::... | Enkripsi pesan keluar (baris 212-224), enkripsi ping keepalive (baris 233-241), dekripsi frame masuk (baris 253-268), drop/zeroization sesi di akhir fungsi (baris 279) | key_size_bits: Tidak terlihat di file ini — kunci transport dikelola internal oleh tipe hasil hs.into_transport() (baris 133, 157), dibungkus variabel `session` (baris 165); nonce_or_iv_size_bytes: Tidak terlihat di file ini — pengelolaan nonce/counter sepenuhnya internal pada tipe transport dari... | snow (dipanggil tidak langsung lewat crate::crypto::handshake::HandshakeSession, diimpor baris 13; tidak ada `use snow::` langsung di file ini); version=snow 0.10.0 (sesuai daft... | src/session/mod.rs:164-279; module=session; symbol=Enkripsi/dekripsi pesan sesi (session.encrypt / session.decrypt di atas EncryptedSession hasil handshake); verdict=CONFIRMED | PARTIAL | MEDIUM | (1) Nonce/counter management sepenuhnya internal ke tipe transport dari snow, tidak terlihat di file ini, sehingga risiko nonce reuse tidak bisa dievaluasi hanya dari session/mod.rs. (2) Tidak ada AAD eksplisit dipakai untuk memisahkan domain TYPE_TEXT/TYPE_BLUR/TYPE_PING — pemisahan jenis pesan terjadi SETELAH dekripsi lewat byte tag pertama pada plainte... |
| CR-027 | Handshake Noise_IK (key agreement) yang diorkestrasi dari run_session | Noise_IK, pola 2 pesan sesuai komentar kode: initiator mengirim '-> e, es, s, ss' (baris 122) lalu membaca '<- e, ee, se' (baris 127-131); responder membaca '<- e, es, s, ss' (baris 138-143, dikomentari '<- e, es, s, ... | run_session, blok handshake Role::Initiator (baris 117-134) dan Role::Responder (baris 135-158) | key_size_bits: 256 bit / 32 byte — local_noise_sk: [u8; 32] (baris 96) dan peer_noise_pk: Option<[u8; 32]> (baris 97), dinyatakan eksplisit pada signature run_session; nonce_or_iv_size_bytes: Tidak terlihat di file ini (bagian dari internal handshake Noise di crypto::handshake); tag_size_bytes: T... | snow (via crate::crypto::handshake::HandshakeSession, diimpor baris 13); version=snow 0.10.0 | src/session/mod.rs:117-159; module=session; symbol=Handshake Noise_IK (key agreement) yang diorkestrasi dari run_session; verdict=CONFIRMED | PARTIAL | MEDIUM | Verifikasi identitas peer (baris 145-151) hanya dijalankan bila peer_noise_pk == Some(..); bila None (kontak belum dikenal), tidak ada pengecekan identitas sama sekali pada level file ini — autentikasi peer bergantung sepenuhnya pada caller yang menyediakan peer_noise_pk yang benar. Enkripsi lewat Noise_IK di sini TIDAK otomatis berarti peer sudah teraute... |
| CR-028 | Representasi kunci Noise sebagai array byte mentah pada boundary fungsi (local_noise_sk, peer_noise_pk) | Representasi byte mentah [u8; 32] (bukan format terstruktur seperti PEM/DER/base64); tidak ada (de)serialisasi eksplisit di file ini selain lewatnya array by-value/by-reference antar fungsi. | Signature run_session (parameter local_noise_sk, peer_noise_pk) | key_size_bits: 256 bit / 32 byte, sesuai deklarasi tipe pada baris 96-97; nonce_or_iv_size_bytes: Tidak berlaku; tag_size_bytes: Tidak berlaku; associated_data_desc: Tidak berlaku | Tidak ada crate serialisasi dipakai langsung di file ini (mis. serde/base64/hex) — murni tipe primitif Rust [u8; 32]; version=Tidak berlaku | src/session/mod.rs:96-97; module=session; symbol=Representasi kunci Noise sebagai array byte mentah pada boundary fungsi (local_noise_sk, peer_noise_pk); verdict=CONFIRMED | NEEDS_CONFIRMATION | MEDIUM | Karena local_noise_sk berupa [u8;32] polos (bukan tipe secret/zeroizing) pada parameter fungsi ini, setelah run_session selesai penanganan memorinya bergantung pada drop biasa Rust (dealokasi, TIDAK dijamin di-overwrite nol) kecuali zeroization ditangani di tempat lain (mis. tipe pemanggil sebelum memanggil run_session, tidak dibaca dalam audit ini) — fil... |
| CR-029 | Fingerprint Ed25519 (hex string, diperlakukan opaque) | Ed25519 — disebut hanya di komentar sebagai sumber fingerprint (hex 64 char = 32 byte pubkey); tidak ada pemanggilan API pembangkitan/verifikasi tanda tangan di 3 file yang diaudit. | Dipakai sebagai identifier untuk (a) TXT record mDNS agar initiator bisa mencocokkan peer dengan kontak dikenal (lan.rs baris 90, 128-131, 142-145), dan (b) penentuan Role::Initiator/Responder via perbandingan string ... | key_size_bits: Tidak dapat dipastikan dari file ini secara langsung — komentar advertise() menyebut 'Fingerprint Ed25519 hex panjangnya 64 char' (lan.rs baris 84-86), konsisten dengan representasi hex dari digest/pubkey 32 byte (256 bit), tetapi derivasi/format pastinya tidak terlihat di 3 file y... | ed25519-dalek 2.2.0 (versi ter-resolve project menurut Cargo.lock) — TIDAK dipanggil/di-import di src/transport/lan.rs, mod.rs, atau frame.rs; fingerprint di sini hanya berupa &... | src/transport/lan.rs:5-90; module=transport_lan; symbol=Fingerprint Ed25519 (hex string, diperlakukan opaque); verdict=CONFIRMED | DOCUMENTED_ONLY | HIGH | (1) role_from_fp (mod.rs 101-107) membandingkan fingerprint pakai operator `<` bawaan &str (byte-wise, non-constant-time) — bukan operasi kriptografi rahasia, dan risiko timing side-channel dinilai rendah karena kedua fingerprint sudah publik/diketahui kedua pihak, namun tidak ada penjelasan eksplisit di kode soal pertimbangan ini. (2) Fingerprint diiklan... |
| CR-030 | Tidak ada primitif kriptografi yang dipanggil langsung di file ini | N/A — tidak ada import/pemakaian langsung AEAD/hash/KDF/password-hashing/signature/key-agreement/MAC/CSPRNG/zeroize di src/transport/tor.rs. Manajemen kunci onion service Tor v3 (yang menurut spesifikasi publik Tor be... | Bootstrap Tor client, hosting onion service, accept/connect stream — seluruhnya operasi jaringan/orkestrasi, bukan operasi kriptografi langsung |  | arti-client 0.43.0, tor-hsservice 0.43.0, tor-cell 0.43.0, safelog 0.8.2 (semua dipakai hanya untuk orkestrasi client/onion-service/error, bukan pemanggilan primitif kripto eksp... | src/transport/tor.rs:1-122; module=transport_tor; symbol=Tidak ada primitif kriptografi yang dipanggil langsung di file ini; verdict=CORRECTED | NOT_FOUND | HIGH | Karena tidak ada primitif kripto yang tampak langsung di file ini, analisis nonce-reuse/key-reuse/domain-separation/zeroization tidak bisa dilakukan dari file ini saja — perlu audit terpisah pada crate tor-hsservice/arti-client itu sendiri (di luar cakupan file yang diberikan). Klaim inti (tidak ada import/pemakaian AEAD/hash/KDF/password-hashing/signatur... |
| CR-031 | Ed25519 identity public key extraction | Ed25519 public key bytes (bundle.identity.public_key().to_bytes()) | build_self_keys() extracts the identity public key to populate SelfKeys.ed25519_pub, used downstream for the fingerprint and invite-code encoding shown in the TUI | key_size_bits: 256 (ed25519_pub: [u8; 32], struct field at mod.rs:36) | ed25519-dalek 2.2.0 (per resolved Cargo.lock) — not directly imported in tui/mod.rs; accessed only via crate::identity::keypair::KeyBundle | src/tui/mod.rs:281-292; module=tui; symbol=Ed25519 identity public key extraction; verdict=CONFIRMED | PARTIAL | MEDIUM | Only public-key bytes are handled in this file, so no direct secret-exposure risk from this code path. Signing/verification logic and any domain-separation for Ed25519 signatures are not present in tui/mod.rs and could not be assessed. build_self_keys (mod.rs:281-292) matches exactly: ed_pub = bundle.identity.public_key().to_bytes() at line 282. Struct fi... |
| CR-032 | Noise/X25519 static keypair (secret + public) | X25519 static Diffie-Hellman keypair, later consumed by a Noise-pattern handshake inside crate::session::run_session (not visible in this file); the pattern name (e.g. Noise_IK) is not spelled out anywhere in tui/mod.... | local_sk (=keys.noise_sk) and peer_pk (=contact.noise_pub) are passed into session::run_session at start_connection to drive the peer handshake/session in the session module | key_size_bits: 256 (noise_sk: [u8;32], noise_pub: [u8;32]; struct fields at mod.rs:34-35) | x25519-dalek 2.0.1 (per resolved Cargo.lock) — not directly imported in tui/mod.rs; accessed only via crate::identity::keypair::KeyBundle.noise | src/tui/mod.rs:32-38; module=tui; symbol=Noise/X25519 static keypair (secret + public); verdict=CONFIRMED | PARTIAL | MEDIUM | Static-key reuse across multiple sessions is expected/by-design for a Noise static key, not inherently a flaw, but this file shows no rotation mechanism. noise_sk is not wrapped in a zeroizing type here, so it is not scrubbed from memory on drop within this file's code. Nonce handling, handshake pattern correctness, and domain separation all live in the s... |
| CR-033 | Identity vault seal/unseal (passphrase-derived) | Not verified from tui/mod.rs — only opaque calls to vault::read_vault/vault::unseal/vault::seal/vault::write_vault are visible. Project-level CLAUDE.md states Argon2id + ChaCha20-Poly1305 for the vault, but that was n... | try_unlock() unseals an existing vault with app.pass_input bytes; create_vault() seals a freshly generated KeyBundle with app.pass_input bytes and writes it to disk | key_size_bits: Unknown from this file; nonce_or_iv_size_bytes: Unknown from this file; tag_size_bytes: Unknown from this file | Candidates from resolved Cargo.lock: argon2 0.5.3, chacha20poly1305 0.10.1 — NOT confirmed as used here from these two files alone | src/tui/mod.rs:559-592; module=tui; symbol=Identity vault seal/unseal (passphrase-derived); verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | Passphrase strings are not wrapped in a zeroizing type in this file. Algorithm-level misuse risk (KDF cost parameters, nonce management, AEAD tag verification) cannot be assessed from tui/mod.rs since the vault primitive implementation lives in identity::vault, which was outside this review's assigned scope. Honestly scoped as NEEDS_CONFIRMATION/LOW since... |
| CR-034 | Contacts store symmetric key (derive_contacts_key) | Not verified from tui/mod.rs — contacts::derive_contacts_key(&bundle) is an opaque call; its implementation was not part of the files read for this task | Derived once at unlock/create time from the identity KeyBundle; passed as the key argument to contacts::load_contacts/save_contacts for the on-disk contacts store | key_size_bits: 256 (contacts_key: Option<[u8; 32]>, mod.rs:182) | Unconfirmed from these two files; blake2 0.10.6 appears in the resolved Cargo.lock as a candidate but this is not established by tui/mod.rs or tui/ui.rs | src/tui/mod.rs:182-182; module=tui; symbol=Contacts store symmetric key (derive_contacts_key); verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | Whether load_contacts/save_contacts use this key with an AEAD (nonce handling, key reuse across repeated saves) cannot be assessed from tui/mod.rs since the contacts module implementation was outside this review's assigned scope. contacts_key: Option<[u8; 32]> confirmed at mod.rs:182. Derivation call sites confirmed at try_unlock (mod.rs:569) and create_v... |
| CR-035 | Identity fingerprint (contacts::fingerprint) | Not verified from tui/mod.rs/ui.rs — opaque call contacts::fingerprint(ed25519_pub, noise_pub); blake2 0.10.6 appears in the resolved Cargo.lock as a candidate hash crate but is not confirmed by the two files read |  |  | Unconfirmed from these files (candidate: blake2 0.10.6) | src/tui/mod.rs:286-286; module=tui; symbol=Identity fingerprint (contacts::fingerprint); verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | The fingerprint is explicitly used for manual out-of-band verification by the user (ui.rs comment, lines 495-497: it 'HARUS' bind both keys because it's what the user compares out-of-band). This file only formats/displays the value; the hash algorithm's collision resistance and domain separation between the two 32-byte inputs cannot be assessed since cont... |
| CR-036 | Key-generation entropy source (KeyBundle::generate) | Not verified from tui/mod.rs — opaque call KeyBundle::generate(); the RNG source is not visible in this file. rand 0.8.6 appears in the resolved Cargo.lock as a candidate. | Called once when creating a brand-new identity vault (create_vault, mod.rs:583-592) to produce a fresh Ed25519 + X25519 KeyBundle |  | Unconfirmed from these files (candidate: rand 0.8.6) | src/tui/mod.rs:584-584; module=tui; symbol=Key-generation entropy source (KeyBundle::generate); verdict=CONFIRMED | NEEDS_CONFIRMATION | LOW | RNG quality/seeding and reuse cannot be assessed from tui/mod.rs since KeyBundle::generate()'s internals were outside this review's assigned scope. Call site confirmed at mod.rs:584 inside create_vault. Appropriately scoped as NEEDS_CONFIRMATION/LOW since RNG internals are not visible in tui/mod.rs. |

## Temuan Kripto Utama

| Area | Klaim | Evidence | Status | Confidence |
|------|-------|----------|--------|------------|
| Vault identitas | Vault melindungi secret Ed25519 dan X25519 dengan Argon2id plus ChaCha20-Poly1305; format fixed-size tidak memiliki header/magic/version dan error dekripsi dibuat ambigu. | CR-013: src/identity/vault.rs:60-128; symbol=ChaCha20-Poly1305 AEAD (vault encryption)<br>CR-014: src/identity/vault.rs:39-56; symbol=Argon2id password-based key derivation<br>CR-015: src/identity/keypair.rs:6-38; symbol=Ed25519 identity keypair (signing/verifying key)<br>CR-018: src/identity/vault.rs:74-75; symbol=ChaCha20Poly1305 96-bit random nonce<br>CR-019: src/identity/vault.rs:1-31; symbol=Vault binary layout / raw secret-key serialization | IMPLEMENTED/PARTIAL | HIGH |
| Handshake dan transport sesi | Noise_IK via snow mengorkestrasi X25519, ChaChaPoly, dan BLAKE2s; wrapper aplikasi tidak mengelola nonce secara manual dan error snow dipetakan seragam. | CR-007: src/crypto/handshake.rs:21-47; symbol=X25519 Diffie-Hellman (Noise DH token es/ss/ee/se) via snow 0.10.0<br>CR-008: src/crypto/handshake.rs:21-98; symbol=ChaCha20-Poly1305 (Noise 'ChaChaPoly') untuk handshake payload & transport messages<br>CR-009: src/crypto/handshake.rs:21-21; symbol=BLAKE2s sebagai fungsi hash Noise (transcript hash / HKDF)<br>CR-010: src/crypto/handshake.rs:21-21; symbol=Noise HKDF (key derivation dari DH output, berbasis BLAKE2s) via snow 0.10.0<br>CR-011: src/crypto/handshake.rs:32-46; symbol=Key serialization kunci Noise sebagai raw byte array [u8;32] | NEEDS_CONFIRMATION | LOW |
| Fingerprint dan invite | Fingerprint mengikat public key Ed25519 dan X25519 dengan BLAKE2s256; invite adalah serialisasi base64 public key, bukan tanda tangan/otentikasi kriptografis. | CR-002: src/contacts/mod.rs:48-54; symbol=BLAKE2s256 identity fingerprint<br>CR-005: src/contacts/mod.rs:59-92; symbol=Base64 URL-safe (no padding) invite code encoding<br>CR-006: src/contacts/mod.rs:114-138; symbol=Hex encoding untuk representasi key (fingerprint output & format plaintext kontak) | IMPLEMENTED/PARTIAL | HIGH |
| Contact store | Contact store memakai ChaCha20-Poly1305 dengan nonce random 96-bit dan key deterministik dari identity secret via BLAKE2s; tidak ada AAD atau versi format. | CR-001: src/contacts/mod.rs:170-198; symbol=ChaCha20Poly1305 contact-store encryption<br>CR-003: src/contacts/mod.rs:104-112; symbol=BLAKE2s256-based contacts key derivation<br>CR-004: src/contacts/mod.rs:173-176; symbol=OsRng CSPRNG untuk nonce ChaCha20Poly1305 | IMPLEMENTED/PARTIAL | MEDIUM |
| Zeroization | ZeroizeOnDrop terlihat pada key material identity, tetapi beberapa boundary aplikasi masih memakai String/[u8;32]/Vec biasa tanpa zeroization eksplisit. | CR-001: src/contacts/mod.rs:170-198; symbol=ChaCha20Poly1305 contact-store encryption<br>CR-003: src/contacts/mod.rs:104-112; symbol=BLAKE2s256-based contacts key derivation<br>CR-013: src/identity/vault.rs:60-128; symbol=ChaCha20-Poly1305 AEAD (vault encryption)<br>CR-016: src/identity/keypair.rs:40-72; symbol=X25519 Noise keypair (Diffie-Hellman key agreement)<br>CR-022: src/main.rs:189-190; symbol=X25519 (Noise) key material access (public_bytes/secret_bytes) | IMPLEMENTED/PARTIAL | MEDIUM |
